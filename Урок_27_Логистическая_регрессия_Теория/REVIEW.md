# REVIEW — Урок 27. Логистическая регрессия (теория)

Источник: глубокий разбор Opus 5. Правки по этому отчёту **применены** (F1–F35).

---

# Ревью: `Урок_27_Логистическая_регрессия.ipynb`

Проверено на sklearn **1.8.0** / Python 3.14. Все числа, помеченные «проверено», получены прогоном кода.
Нумерация ячеек — сквозная по `nb['cells']` (ячейка 0 — заголовок).

---

## 1. Термины: аудит первого появления

| Термин / аббревиатура | Ячейка первого появления | Расшифрован там? |
|---|---|---|
| обучение с учителем | 1 | да |
| классификация / бинарная / многоклассовая | 1 | да |
| кластеризация, регрессия | 1 (таблица) | да (в таблице) |
| шансы / odds | 6 | **да** — хороший пример со Спартаком |
| log (какой логарифм?) | 6 | **нет** — не сказано, что натуральный; дальше вперемешку `log` и `\ln` |
| logit / логит | 8 | да |
| «logistic unit» | 8 | **нет** — английская этимология без русского перевода |
| сигмоида | 8 | да |
| линейная комбинация | 10 | да (из занятия 25) |
| порог / threshold | 11 | да |
| `fit` / `predict` / `predict_proba` / `decision_function` | 13 | да |
| log loss | **16 (в заголовке H2)** | **нет** — расшифровано только в ячейке 18 |
| бинарная кросс-энтропия | 18 | **нет** — назван синоним, смысл не объяснён |
| L2-штраф | 18 | **нет** — введён в занятии 25, здесь без напоминания |
| градиентный спуск | 18 | да (кратко) |
| `solver`, `lbfgs` | 18 | частично |
| квазиньютоновский метод | 18 | **нет** — жаргон для 8-классника |
| train / validation / test | 22 | да |
| `stratify` / стратифицированное разбиение | 22 | да |
| TP / FP / FN / TN | 24 | да (англ. + рус.) |
| матрица ошибок / confusion matrix | 24 | частично — англ. название только внутри имени функции |
| Accuracy / Precision / Recall / F1 | 26 | смысл — да; **русские названия (точность / полнота) — нет** |
| среднее гармоническое | 26 | **нет** |
| baseline | 28 | да («простой соперник») |
| `DummyClassifier`, стратегии | 28 | да (таблица) |
| дисбаланс классов | 30 | да |
| `make_classification` (синтетика) | 31 (код) | **нет** — нигде не сказано, что данные искусственные |
| ранжирование | 35 | да |
| ROC | 35 | частично — англ. расшифровка есть, русского пояснения нет |
| TPR / FPR | 35 | частично — формула есть, **по буквам не раскрыто** |
| AUC | 35 | да |
| PR / precision–recall | 35 | да |
| AP / average precision | 35 | **нет русского перевода** |
| гиперплоскость | 37 | **нет** |
| `StandardScaler` | 41 | **нет** — что именно он делает, не сказано ни разу |
| Ridge / Lasso / ElasticNet | 41 | **нет** — из занятия 25, здесь без напоминания |
| L1 / L2 | 18 и 41 | **нет** |
| `C` | 18, 43 | да («обратная сила») |
| `l1_ratio` | 43 | **да, но описание противоречит коду** (см. F26) |
| `class_weight='balanced'` | 45 | да |
| softmax | 48 (внутри `print`) | **нет** |
| multinomial | 48 (внутри `print`) | **нет** |
| macro / weighted | 47 | да (кратко) |
| `PolynomialFeatures`, `make_moons` | 50 (код) | **нет** |
| «логрег» | 51 | **нет** — жаргонное сокращение |
| экстраполяция / интерполяция | 51 | да |
| калибровка | 52 | да |
| isotonic / Platt | 52 (первое предложение) | **нет в момент первого использования**, расшифровано двумя абзацами ниже |
| Brier score | 52 | да |
| `FrozenEstimator` | 52 | да |
| `CalibratedClassifierCV` | 52 | да (CV не раскрыто — мелочь) |

---

## 2. Findings по ячейкам

### F1 · minor · «Прямая выходит за диапазон» показана в основном вне данных

Ячейки 4–5. Заголовок графика утверждает, что прямая выходит за [0; 1], но сетка `grid_1d` идёт от −2 до 12, а реальные данные — от 1 до 8 часов. Школьник справедливо возразит: «так это же выдуманные часы». На самом деле прогноз отрицателен и **в настоящей точке данных**: при 1 часе прямая даёт −0.159 (проверено).

**Fix:** заменить код ячейки 5:

```python
from sklearn.linear_model import LinearRegression

linear_classifier = LinearRegression().fit(hours.reshape(-1, 1), passed)
grid_1d = np.linspace(-2, 12, 200).reshape(-1, 1)
pred_at_data = linear_classifier.predict(hours.reshape(-1, 1))

plt.figure(figsize=(8, 4))
plt.scatter(hours, passed, c=np.where(passed == 1, 'tab:green', 'tab:red'), s=80, label='наблюдения')
plt.plot(grid_1d, linear_classifier.predict(grid_1d), color='gray', label='линейная регрессия')
plt.axhspan(-0.7, 0, color='tab:red', alpha=0.08)
plt.axhspan(1, 1.7, color='tab:red', alpha=0.08)
plt.axhline(0, color='black', linewidth=0.8)
plt.axhline(1, color='black', linewidth=0.8)
plt.scatter(hours[pred_at_data < 0], pred_at_data[pred_at_data < 0],
            marker='X', s=160, color='black', zorder=4, label='прогноз меньше 0')
plt.xlabel('Часы подготовки'); plt.ylabel('Прогноз')
plt.title('Прямая выходит за диапазон вероятностей')
plt.grid(alpha=0.3); plt.legend(); plt.show()

print('Прогноз прямой в реальных точках данных:')
for h, pr in zip(hours, pred_at_data):
    flag = '  <- вероятность не может быть отрицательной!' if pr < 0 else ''
    print(f'  {h:>4} ч -> {pr:+.3f}{flag}')
```

Печатает, в частности, `1.0 ч -> -0.159` (проверено).

---

### F2 · major · Опечатка и неопределённый логарифм

Ячейка 6: «достаточно воспользоваться **функций** log» — опечатка (нужно «функцией»). Плюс нигде не сказано, какой это логарифм. Дальше в блокноте обозначение скачет: ячейки 6–8 пишут `log`, ячейки 16 и 18 — `ln`. Для 8-классника это два разных значка.

**Fix:** заменить последний абзац ячейки 6:

```markdown
Теперь преобразуем шансы в число от -∞ до +∞: для этого достаточно воспользоваться **логарифмом**.

> Всюду в этом занятии $\log$ и $\ln$ — это **одно и то же**: натуральный логарифм, то есть логарифм по основанию $e \approx 2{,}718$. В коде ему соответствует `np.log`.
```

И привести обозначения к одному виду: в ячейках 7 (подписи графика) и 8 (формула логита) заменить `log` на `\ln` / `ln`.

---

### F3 · minor · Нет числового мостика p → odds → logit → p

Ячейки 6–10. Цепочка «вероятность → шансы → логит → обратно» — центральная идея темы, но она нигде не проходится на одном конкретном числе. Печатается только `log(шансы)` в отрыве от вероятности.

**Fix:** заменить блок `for` в конце ячейки 7 на:

```python
print('Полный путь: вероятность -> шансы -> логит -> обратно вероятность\n')
print(f'{"p":>6} {"odds = p/(1-p)":>16} {"logit = ln(odds)":>18} {"сигмоида(logit)":>17}')
for p_value in [0.02, 0.1, 0.25, 0.5, 0.75, 0.9, 0.98]:
    odds_value = p_value / (1 - p_value)
    logit_value = np.log(odds_value)
    back = 1 / (1 + np.exp(-logit_value))
    print(f'{p_value:>6.2f} {odds_value:>16.3f} {logit_value:>18.2f} {back:>17.2f}')
print('\nПоследний столбец совпал с первым: сигмоида возвращает нас обратно к вероятности.')
```

---

### F4 · minor · «Получившая функция» + непереведённая этимология

Ячейка 8: «**Получившая** функция» — нужно «Получившаяся». И `**log**istic un**it**` оставлено без перевода.

**Fix:** заменить первую строку ячейки 8:

```markdown
Получившаяся функция (логарифм шансов) называется **логит** (logit — от *log*istic *unit*, «логистическая единица»):
```

---

### F5 · minor · Грамматика в ячейке 10

«вернемся к нашей задаче **определение** вероятности» (нужен родительный падеж), «Решать ее мы будем следующим образом» — без знака препинания, «ее» без ё.

**Fix:** заменить первые две строки ячейки 10:

```markdown
Теперь вернёмся к нашей задаче — определению вероятности того, что объект принадлежит классу 1, по его признакам $x_1,\dots,x_p$. Решать её мы будем так.
```

---

### F6 · minor · Ячейка 12 оторвана от модели

Пять вероятностей `[0.12, 0.43, 0.51, 0.78, 0.96]` взяты из воздуха. Идея «порог меняет ответ» показана, но не связана ни с каким объектом, и школьник не видит, что́ именно поменялось.

**Fix:** заменить код ячейки 12:

```python
probabilities = np.array([0.12, 0.43, 0.51, 0.78, 0.96])
print(f'{"P(класс 1)":>11} {"порог 0,5":>11} {"порог 0,8":>11}')
for prob in probabilities:
    print(f'{prob:>11.2f} {int(prob >= 0.5):>11} {int(prob >= 0.8):>11}')
print('\nМодель не переобучалась: изменился только порог, а вероятности те же.')
print('Объект с вероятностью 0,51 при пороге 0,8 переезжает из класса 1 в класс 0.')
```

---

### F7 · major · Заголовок п.6 объединяет два понятия + log loss до расшифровки + сломанный LaTeX

Ячейка 16. Заголовок `## 6. Как обучается модель: log loss и оптимизация *` — два независимых понятия через «и» (нарушение `theory-single-concept-sections`). Плюс аббревиатура **log loss** стоит в заголовке, а расшифровывается только в ячейке 18. Плюс в тексте `$-ln(p)$` и `$ln(p) \le 0$`: без `\ln` LaTeX отрендерит это как произведение переменных *l·n*.

**Fix:** заменить ячейку 16 целиком (переименование раздела, номер не трогаем):

```markdown
## 6. Как обучается модель: функция потерь *

Чтобы модель могла чему-то учиться, нужно уметь измерять, насколько плох её ответ. Такую меру называют **функцией потерь** (loss function) — чем больше её значение, тем хуже прогноз.

Рассмотрим один прогноз. Если правильный класс $y_i=1$, а мы предсказали вероятность этого класса как $p$, то добавим в штраф $-\ln p$. Почему так: раз $p \le 1$, то $\ln p \le 0$, и минус перед ним делает штраф неотрицательным.

Идея проста:

- правильный уверенный прогноз получает маленький штраф;
- неуверенный прогноз получает средний штраф;
- уверенный неправильный прогноз получает очень большой штраф.

Если же правильный класс $y_i=0$, то добавим в штраф $-\ln(1-p)$: теперь «плохо» — это большое $p$.
```

---

### F8 · major · Нерасшифрованный жаргон в ячейке 18

Три места подряд: «бинарная кросс-энтропия» (назван синоним, смысл не раскрыт), «L2-штраф» (введён два занятия назад, здесь без напоминания), «квазиньютоновский метод» (для 8-классника это шум).

**Fix:** заменить два последних абзаца ячейки 18:

```markdown
Получившуюся функцию потерь называют **логарифмическая функция потерь** (*log loss*). У неё есть второе имя — **бинарная кросс-энтропия**: слово «бинарная» здесь значит «для двух классов», а «кросс-энтропия» — принятое в теории информации название для меры расхождения между тем, что модель предсказала, и тем, что случилось на самом деле. Это ровно та же формула, просто под другим именем.

$$L=-\frac1n\sum_{i=1}^n\left[y_i\ln p_i+(1-y_i)\ln(1-p_i)\right].$$

`LogisticRegression` из sklearn по умолчанию минимизирует не просто log loss, а сразу добавляет к нему **L2-штраф** за большие коэффициенты. Напомним из занятия 25: L2 — это штраф вида $\sum w_j^2$, он не даёт весам разрастаться и тем самым мешает модели переобучиться. Силой штрафа управляет параметр `C`; подробнее — в разделе 19.

Минимум ищется **итеративно**: не по готовой формуле, а много раз понемногу улучшая коэффициенты. Знакомый градиентный спуск — простейший такой способ: посчитать направление роста функции и сделать шаг в противоположную сторону. Какой именно алгоритм используется, задаёт параметр `solver`; по умолчанию это `'lbfgs'` — более быстрый родственник градиентного спуска, который сам подбирает длину шага.
```

---

### F9 · major · Градиентный спуск ничего не доказывает

Ячейка 19 крутит 1500 шагов и рисует падающую кривую, но не печатает **ни одного числа**: ни финальных $w$ и $b$, ни того, что они близки к найденным `LogisticRegression`. Утверждение «минимум ищется итеративным методом» остаётся необоснованным — школьник видит просто убывающую линию.

**Fix:** заменить код ячейки 19 (числа ниже проверены):

```python
# Покажем общую идею итеративной оптимизации на простом градиентном спуске.
w_gd, b_gd = 0.0, 0.0
learning_rate = 0.05
loss_history = []
for step in range(1500):
    z = w_gd * hours + b_gd
    p_gd = sigmoid(z)
    eps = 1e-12
    loss = -np.mean(passed * np.log(p_gd + eps) + (1 - passed) * np.log(1 - p_gd + eps))
    loss_history.append(loss)
    w_gd -= learning_rate * np.mean((p_gd - passed) * hours)
    b_gd -= learning_rate * np.mean(p_gd - passed)

plt.figure(figsize=(8, 3.5))
plt.plot(loss_history, label='log loss на каждом шаге')
plt.xlabel('Шаг'); plt.ylabel('Log loss')
plt.title('Ошибка уменьшается шаг за шагом')
plt.grid(alpha=0.3); plt.legend(); plt.show()

print(f'log loss в начале: {loss_history[0]:.4f}')
print(f'log loss в конце:  {loss_history[-1]:.4f}')
print(f'наши коэффициенты после 1500 шагов: w={w_gd:.3f}, b={b_gd:.3f}')

reference = LogisticRegression(C=10_000).fit(hours.reshape(-1, 1), passed)
print(f'что нашёл sklearn (штраф почти выключен): w={reference.coef_[0, 0]:.3f}, '
      f'b={reference.intercept_[0]:.3f}')
print('\nМы идём в ту же сторону, но за 1500 шагов ещё не дошли до конца:')
print('lbfgs внутри sklearn делает это быстрее и точнее.')
```

Вывод (проверено): `0.6931` → `0.3491`, `w=0.794, b=-4.163` против sklearn `w=1.134, b=-6.114`. **Важно:** честно написать, что мы ещё не сошлись, — иначе числа не совпадут и получится новое противоречие.

---

### F10 · major · Не сказано, что данные синтетические и линейные по построению

Ячейки 20–21. `true_score = 0.9*подготовка + 0.55*сон - 7.2 + шум` — данные порождены ровно тем правилом, которое логистическая регрессия и ищет. Отсюда почти идеальные метрики дальше (ROC-AUC на test = 0.993, проверено). Школьник этого не знает и уносит вывод «логрегрессия почти всегда даёт 0.97».

**Fix:** добавить абзац в конец ячейки 20:

```markdown
Данные мы **придумаем сами**, а не возьмём настоящие. Так удобнее учиться: мы заранее знаем правило, по которому ставится ответ, и можем проверить, найдёт ли его модель. Правило будет линейным — «чем больше подготовка и сон, тем выше шанс сдать» — плюс случайный шум. Из-за этого модель дальше покажет очень высокие метрики: мы буквально дали ей задачу того типа, который она умеет решать. На настоящих данных так почти никогда не бывает.
```

---

### F11 · blocker · Неверная перекрёстная ссылка на раздел

Ячейка 22: «тестовую выборку откладываем сейчас и впервые используем только **в разделе 13**». Раздел 13 — «Как порог меняет ошибки», он работает с validation. Test впервые открывается в ячейке 36, которая находится под заголовком **раздела 15**. Ученик, действующий по инструкции, придёт не туда.

**Fix:** заменить абзац в ячейке 22 (с учётом F17, где test-отчёт переезжает в п.14):

```markdown
Test похож на запечатанный вариант контрольной: если после просмотра test менять модель или порог, test перестаёт быть честной проверкой. Поэтому тестовую выборку откладываем сейчас и впервые открываем только в разделе 14.
```

---

### F12 · major · Крошечные validation и test без предупреждения

Ячейка 23 печатает `train=112, val=38, test=50` (проверено) и на этом останавливается. Дальше по этим 38 объектам выбирается порог, а по 50 — делается финальный вывод. Одна ошибка на test — это 2 процентных пункта accuracy. Без этой оговорки ученик поверит, что разница 0.906 против 0.926 что-то значит.

**Fix:** добавить в конец кода ячейки 23:

```python
print('\nВыборки получились маленькие — это важно помнить:')
print(f'  одна ошибка на validation меняет accuracy на {1/len(y_val):.3f}')
print(f'  одна ошибка на test       меняет accuracy на {1/len(y_test):.3f}')
print('Поэтому разницу в метриках в пару сотых нельзя считать настоящей разницей.')
```

---

### F13 · minor · «Матрица ошибок» без английского имени

Ячейка 24. В коде сразу идёт `confusion_matrix` и `ConfusionMatrixDisplay`, но связь русского и английского названий нигде не проговорена.

**Fix:** заменить первую строку ячейки 24:

```markdown
## 9. Матрица ошибок

**Матрица ошибок** (*confusion matrix*, дословно «матрица путаницы») — таблица, которая показывает не только сколько ошибок сделала модель, но и **какие именно**. Используем прогнозы модели на **validation** из раздела 8 — эти объекты не участвовали в обучении коэффициентов.
```

---

### F14 · minor · Метрики без русских названий, «среднее гармоническое» без объяснения, вопрос без ответа

Ячейка 26. Precision и recall по-русски называются «точность» и «полнота» — ученик встретит эти слова в любом другом источнике. «Среднее гармоническое» вводится как известное. Вопрос «подумайте, почему не использовать min вместо F1» задан и нигде не отвечен даже намёком.

**Fix:** заменить список в ячейке 26:

```markdown
- **Accuracy** (*доля правильных ответов*) — какая доля всех ответов верна.
- **Precision** (по-русски *точность*) — как часто модель права, когда говорит «класс 1».
- **Recall** (по-русски *полнота*) — какую долю настоящих объектов класса 1 модель нашла.
- **F1** — **среднее гармоническое** precision и recall. Обычное среднее двух чисел — это их полусумма; среднее гармоническое считается по формуле выше и всегда ближе к меньшему из двух. Поэтому провал по любой из двух метрик тянет F1 вниз, и модель вынуждена держать баланс.

> **Подумайте.** Можно было бы вместо F1 брать просто минимум из precision и recall — он тоже наказывает за провал. Чем F1 удобнее? Подсказка: сравните, как поведут себя min и F1, если precision вырастет с 0,6 до 0,8, а recall останется 0,5.
```

---

### F15 · minor · В п.11 смешаны доли класса в train и в validation

Ячейка 28: «самый частый класс — это как раз класс 1 (его доля 0,66)». `most_frequent` выбирает самый частый класс **по train**, где доля = 0.643; 0,66 — это доля в validation (проверено: train 0.643, val 0.658).

**Fix:** заменить последний абзац ячейки 28:

```markdown
Обратите внимание на подвох в выводе ниже. Самый частый класс в **train** — это как раз класс 1, поэтому `most_frequent` ставит единицу всем подряд. На **validation**, где доля класса 1 равна 0,66, у такого baseline recall получается равным 1,00: он не пропустил ни одного объекта класса 1 просто потому, что никого не отнёс к классу 0. При этом accuracy и precision у него равны той самой доле 0,66 — он угадывает ровно так часто, как часто встречается класс 1. Вывод: любую отдельную метрику можно «купить» тривиальным ответом, поэтому baseline сравнивают сразу по нескольким метрикам.
```

---

### F16 · major · П.12 дублирует п.11, а свой собственный тезис не показывает

Ячейки 30–31. После добавления baseline в п.11 два раздела делают почти одинаковое: `DummyClassifier` против `LogisticRegression`, четыре метрики, `pd.DataFrame`. При этом три пункта самого п.12 («смотреть доли», «не ограничиваться accuracy», «выбирать метрику по цене FP и FN») — третий не показан вообще. Плюс заголовок объединяет два понятия через «и», плюс нигде не сказано, что данные искусственные.

**Fix:** заменить ячейку 30:

```markdown
## 12. Дисбаланс классов: почему accuracy обманывает

Если 95% писем не являются спамом, модель «всегда не спам» получит accuracy 95%, хотя не найдёт ни одного спам-письма. Такой перекос называется **дисбалансом классов**.

В разделе 11 классы были почти поровну, и baseline честно проигрывал по всем метрикам сразу. Сейчас посмотрим, что меняется, когда класс 1 редкий. Данные снова придумаем сами: функция `make_classification` из sklearn генерирует случайную задачу классификации, а параметр `weights=[0.92, 0.08]` задаёт, что класса 1 будет всего 8%.

Смотрите на таблицу так: сравните **accuracy** baseline и модели, а потом — их **recall класса 1**. Разница в accuracy окажется маленькой, а разница в recall — огромной. Именно поэтому на несбалансированных данных accuracy не годится как главная метрика.

Правило: при дисбалансе сначала смотрят доли классов, затем выбирают метрику по цене ошибок — что дороже, пропуск (FN) или ложная тревога (FP).
```

И убрать повторный `from sklearn.dummy import DummyClassifier` из ячейки 31 (он уже импортирован в ячейке 29).

---

### F17 · blocker · П.14 — раздел в одну строчку, а финальная проверка на test происходит не в нём

Ячейка 34 — это две фразы без единого числа, графика или кода. Раздел называется «Первый честный test», но собственно первое открытие test физически происходит в ячейке 36, то есть уже внутри раздела 15 про ROC/PR. Кульминация всего блокнота растворена в разделе про кривые.

**Fix, часть 1** — заменить ячейку 34:

```markdown
## 14. Первый честный test

Все решения приняты: модель обучена на train, порог выбран на validation, метрики выбраны. Теперь **один раз** открываем test и смотрим, что получилось. Повторные правки после просмотра test делают его оценку слишком оптимистичной — подробнее в следующей теме.

Ниже мы применяем к test **ровно тот порог**, который выбрали в разделе 13, и сравниваем метрики на validation и на test. Если бы на test было заметно хуже, это был бы сигнал, что мы подогнали порог под 38 валидационных объектов.
```

**Fix, часть 2** — вставить **новую кодовую ячейку сразу после ячейки 34** (числа проверены):

```python
p_test = model.predict_proba(X_test)[:, 1]
y_test_pred = (p_test >= best_threshold).astype(int)
y_val_pred_t = (p_val >= best_threshold).astype(int)

print(f'Порог, выбранный на validation: {best_threshold:.2f}\n')
final_report = pd.DataFrame([
    {'выборка': 'validation',
     'accuracy': accuracy_score(y_val, y_val_pred_t),
     'precision': precision_score(y_val, y_val_pred_t, zero_division=0),
     'recall': recall_score(y_val, y_val_pred_t, zero_division=0),
     'F1': f1_score(y_val, y_val_pred_t, zero_division=0)},
    {'выборка': 'test',
     'accuracy': accuracy_score(y_test, y_test_pred),
     'precision': precision_score(y_test, y_test_pred, zero_division=0),
     'recall': recall_score(y_test, y_test_pred, zero_division=0),
     'F1': f1_score(y_test, y_test_pred, zero_division=0)},
])
print(final_report.round(3).to_string(index=False))
print(f'\nОбъектов в test всего {len(y_test)}: одна ошибка меняет accuracy на {1/len(y_test):.2f},')
print('поэтому разницу в пару сотых между validation и test считать улучшением нельзя.')
```

Печатает (проверено): validation `0.895 / 0.862 / 1.000 / 0.926`, test `0.960 / 0.941 / 1.000 / 0.970`.

**Fix, часть 3** — из ячейки 36 удалить первые пять строк (`p_test = ...` … `Test F1`), она теперь только про кривые. Полная новая ячейка 36 — в F20.

---

### F18 · major · Главное утверждение п.15 нигде не показано

Ячейка 35. Таблица заявляет: «при сильном дисбалансе ROC выглядит слишком оптимистично, PR честнее показывает работу с редким классом». Это самый ценный вывод раздела — и он не подкреплён ничем: единственный график строится на почти идеальной сбалансированной задаче (ROC-AUC = 0.993, доля класса 1 = 0.64, проверено), где никакой разницы между ROC и PR увидеть невозможно.

**Fix:** добавить **новую кодовую ячейку сразу после ячейки 36** (все числа проверены):

```python
# Проверим главный тезис: при редком классе ROC приукрашивает, а PR — нет.
X_rare, y_rare = make_classification(
    n_samples=4000, n_features=5, n_informative=3, n_redundant=0,
    weights=[0.97, 0.03], class_sep=0.6, flip_y=0.01, random_state=42,
)
Xr_train, Xr_control, yr_train, yr_control = train_test_split(
    X_rare, y_rare, test_size=0.3, random_state=42, stratify=y_rare
)
rare_model = LogisticRegression(max_iter=1000).fit(Xr_train, yr_train)
p_rare = rare_model.predict_proba(Xr_control)[:, 1]

share = yr_control.mean()
auc_rare = roc_auc_score(yr_control, p_rare)
ap_rare = average_precision_score(yr_control, p_rare)
fpr_r, tpr_r, _ = roc_curve(yr_control, p_rare)
prec_r, rec_r, _ = precision_recall_curve(yr_control, p_rare)

fig, axes = plt.subplots(1, 2, figsize=(12, 4.5))
axes[0].plot(fpr_r, tpr_r, linewidth=2, label=f'ROC-AUC = {auc_rare:.3f}')
axes[0].plot([0, 1], [0, 1], '--', color='gray', label='случайная модель, AUC = 0,5')
axes[0].set_xlabel('FPR — доля ложных тревог'); axes[0].set_ylabel('TPR = recall')
axes[0].set_title('ROC: кривая выглядит прилично')
axes[0].legend(); axes[0].grid(alpha=0.3)

axes[1].plot(rec_r, prec_r, linewidth=2, color='tab:orange', label=f'AP = {ap_rare:.3f}')
axes[1].axhline(share, linestyle='--', color='gray', label=f'случайная модель = {share:.3f}')
axes[1].set_xlabel('Recall'); axes[1].set_ylabel('Precision')
axes[1].set_title('PR: precision обваливается')
axes[1].legend(); axes[1].grid(alpha=0.3)
plt.tight_layout(); plt.show()

print(f'Класс 1 встречается в {share:.1%} случаев.')
print(f'ROC-AUC = {auc_rare:.3f} — звучит как хорошая модель.')
print('А теперь посмотрим, какой ценой она ловит редкий класс:')
for target in [0.5, 0.8]:
    print(f'  чтобы поймать {target:.0%} редкого класса, precision падает до '
          f'{prec_r[rec_r >= target].max():.3f}')
print('\nПри recall 80% почти 9 из 10 сработок — ложная тревога.')
print('ROC этого не показал, потому что нулей очень много и знаменатель FPR огромный.')
```

Печатает (проверено): доля 0.033, ROC-AUC **0.861**, AP **0.528**, precision при recall 50% = **0.457**, при 80% = **0.131**.

---

### F19 · minor · Аббревиатуры TPR / FPR / AP / ROC не раскрыты по буквам

Ячейка 35. `TPR` и `FPR` даны формулой и русским описанием, но сами буквы не расшифрованы, хотя TP/FP/TN/FN ученик выучил в п.9 — связь стоит показать прямо. `AP` не переведён. «receiver operating characteristic» дано по-английски и для 8-классника бессмысленно.

**Fix:** заменить соответствующие фрагменты ячейки 35:

```markdown
**ROC-кривая** (*receiver operating characteristic* — исторический термин из радиолокации 1940-х годов, «характеристика работы приёмника»; переводить его не принято, просто запомните сокращение) по каждому порогу откладывает:

- по оси Y — **TPR** (*True Positive Rate*, доля верно найденных единиц) $=\frac{TP}{TP+FN}$. Это в точности **recall**: какую долю настоящих единиц модель поймала;
- по оси X — **FPR** (*False Positive Rate*, доля ложных тревог) $=\frac{FP}{FP+TN}$: какую долю настоящих нулей модель ошибочно назвала единицами.

Обратите внимание: обе дроби собраны из тех же TP, FP, FN, TN, что и матрица ошибок в п.9, — просто для каждого порога она своя.
```

и

```markdown
Как читать: чем выше и правее, тем лучше. Уровень «случайной» модели здесь не диагональ, а горизонтальная линия на высоте доли класса 1. Площадь под кривой называют **AP** (*average precision*, «средняя точность») — одним числом она говорит, насколько высоко кривая проходит.
```

---

### F20 · major · Подписи осей ROC не совпадают с текстом; результат на test выше, чем на validation, без комментария

Ячейка 36. Текст п.15 три раза говорит «FPR» и «TPR», а на графике подписано «Доля ложных тревог» и «Recall» — ученик не свяжет одно с другим. Кроме того, на 50 тестовых объектах ROC-кривая состоит всего из 6 точек (проверено) — это ломаная из трёх отрезков, на которой «площадь под кривой» плохо читается, и об этом не сказано.

**Fix:** заменить код ячейки 36 целиком (после переноса test-отчёта в п.14, см. F17):

```python
from sklearn.metrics import (roc_curve, roc_auc_score,
                             precision_recall_curve, average_precision_score)

fpr, tpr, _ = roc_curve(y_test, p_test)
precision_curve, recall_curve, _ = precision_recall_curve(y_test, p_test)

fig, axes = plt.subplots(1, 2, figsize=(12, 4.5))
axes[0].plot(fpr, tpr, marker='o', linewidth=2,
             label=f'ROC-AUC = {roc_auc_score(y_test, p_test):.3f}')
axes[0].plot([0, 1], [0, 1], '--', color='gray', label='случайная модель')
axes[0].set_xlabel('FPR — доля ложных тревог')
axes[0].set_ylabel('TPR = recall — доля пойманных единиц')
axes[0].set_title('ROC-кривая на test')
axes[0].legend(); axes[0].grid(alpha=0.3)

axes[1].plot(recall_curve, precision_curve, marker='o', linewidth=2,
             label=f'AP = {average_precision_score(y_test, p_test):.3f}')
axes[1].axhline(y_test.mean(), linestyle='--', color='gray',
                label=f'доля класса 1 = {y_test.mean():.2f}')
axes[1].set_xlabel('Recall'); axes[1].set_ylabel('Precision')
axes[1].set_title('Precision–Recall кривая на test')
axes[1].legend(); axes[1].grid(alpha=0.3)
plt.tight_layout(); plt.show()

print(f'Точек-порогов на ROC-кривой всего {len(fpr)}: в test лишь {len(y_test)} объектов,')
print('поэтому кривая — короткая ломаная, а не плавная линия.')
print(f'ROC-AUC = {roc_auc_score(y_test, p_test):.3f} — почти идеально, но помните раздел 7:')
print('мы сами придумали данные по линейному правилу, а модель ищет именно линейное правило.')
```

Проверено: ROC-AUC = 0.993, AP = 0.996, 6 точек на кривой.

---

### F21 · minor · «Гиперплоскость» без расшифровки

Ячейка 37, последняя фраза.

**Fix:** заменить последнюю строку ячейки 37:

```markdown
При большем числе признаков получается **гиперплоскость** — это просто общее слово для «плоского» разделителя в пространстве любой размерности: при двух признаках это прямая, при трёх — обычная плоскость, при четырёх и больше нарисовать её уже нельзя, но формула $w_1x_1+\ldots+w_px_p+b=0$ остаётся той же.
```

---

### F22 · minor · График границы решений без сетки и без легенды

Ячейка 38. Нет `plt.grid`, и нигде не сказано, что точки — это **test** и что их цвет означает настоящий класс. Точки красятся тем же `RdYlGn`, что и фон, поэтому неверно предсказанный объект визуально сливается с фоном. Кроме того, рисовать здесь именно test странно: раздел иллюстративный, а train честнее.

**Fix:** заменить код ячейки 38:

```python
x1 = np.linspace(X['подготовка'].min(), X['подготовка'].max(), 160)
x2 = np.linspace(X['сон'].min(), X['сон'].max(), 160)
xx1, xx2 = np.meshgrid(x1, x2)
grid_2d = pd.DataFrame({'подготовка': xx1.ravel(), 'сон': xx2.ravel()})
surface = model.predict_proba(grid_2d)[:, 1].reshape(xx1.shape)

plt.figure(figsize=(8, 5.5))
plt.contourf(xx1, xx2, surface, levels=np.linspace(0, 1, 11), cmap='RdYlGn', alpha=0.35)
plt.contour(xx1, xx2, surface, levels=[0.5], colors='black', linewidths=2)
for label, colour, name in [(0, 'tab:red', 'не сдал (факт)'), (1, 'tab:green', 'сдал (факт)')]:
    mask = (y_train == label)
    plt.scatter(X_train['подготовка'][mask], X_train['сон'][mask],
                c=colour, edgecolor='black', s=45, label=name)
plt.colorbar(label='P(класс 1)')
plt.xlabel('Часы подготовки'); plt.ylabel('Часы сна')
plt.title('Чёрная линия — граница решений p = 0,5 (точки: обучающая выборка)')
plt.grid(alpha=0.2); plt.legend(loc='lower left'); plt.show()
```

---

### F23 · major · П.17 не связывает коэффициенты с шансами, хотя шансы введены в п.3

Ячейки 39–40. Это главный пробел содержания. Логистическая регрессия линейна **не по вероятности, а по логиту** — то есть коэффициент $w_j$ означает «прибавка к $\ln(\text{шансы})$», а $e^{w_j}$ — «во столько раз умножаются шансы». Ровно эта конструкция была построена в п.3 и здесь не используется. В итоге п.17 говорит только чего **нельзя**, и ни одного правила, как **можно**. Таблица коэффициентов (ячейка 40) печатает два числа без интерпретации.

**Fix, часть 1** — вставить абзац в ячейку 39 перед «Один и тот же прирост $z$…»:

```markdown
А что коэффициент означает **точно**? Вспомним п.3: модель считает $z=\log(\text{шансы})$. Значит, если признак $x_j$ вырос на единицу, то $z$ вырос на $w_j$, а шансы **умножились** на $e^{w_j}$. Это и есть точная формулировка:

$$\text{новые шансы} = \text{старые шансы}\cdot e^{w_j}.$$

Величину $e^{w_j}$ называют **отношением шансов** (*odds ratio*). Если $e^{w_j}=2$, то каждый дополнительный час подготовки удваивает шансы сдать.
```

**Fix, часть 2** — заменить код ячейки 40 (числа проверены):

```python
coef_table = pd.DataFrame({
    'признак': X.columns,
    'коэффициент w': model.coef_[0],
    'во сколько раз растут шансы, exp(w)': np.exp(model.coef_[0]),
})
print(f'Свободный член b = {model.intercept_[0]:.3f}')
print('Читаем так: +1 час подготовки умножает шансы сдать примерно на '
      f'{np.exp(model.coef_[0][0]):.1f}, а +1 час сна — примерно на '
      f'{np.exp(model.coef_[0][1]):.1f}.')
print('Оба признака помогают, но подготовка — заметно сильнее.')
coef_table.round(3)
```

Проверено: $w$ = 1.313 и 0.472, $e^w$ = **3.718** и **1.604**, $b$ = −7.805.

---

### F24 · major · В п.18 механизм заявлен, но не показан

Ячейки 41–42. Текст объясняет ровно правильную вещь: «коэффициент должен стать большим — а регуляризация прижимает большие коэффициенты к нулю». Но код печатает **только accuracy**. Ключевое звено — сам коэффициент — не показано, и вывод повисает в воздухе. Проверено: коэффициент подготовки в часах = 0.786, в сутках = 0.239, тогда как для сохранения смысла ему нужно было бы стать ≈ 18.9 (= 0.786 × 24). Модель в «сутках» предсказывает класс 1 всем 38 объектам validation — то есть скатывается ровно к тривиальному ответу.

**Fix:** заменить код ячейки 42 (все числа проверены):

```python
from sklearn.preprocessing import StandardScaler

# Та же подготовка, только записанная в сутках: 5 часов -> 0.21.
X_train_days = X_train.copy()
X_val_days = X_val.copy()
X_train_days['подготовка'] = X_train_days['подготовка'] / 24
X_val_days['подготовка'] = X_val_days['подготовка'] / 24

variants = [('подготовка в часах ', X_train, X_val),
            ('подготовка в сутках', X_train_days, X_val_days)]

print('Без масштабирования:')
for name, X_tr, X_v in variants:
    raw_model = LogisticRegression(C=0.05).fit(X_tr, y_train)
    pred = raw_model.predict(X_v)
    print(f'  {name}: коэффициент подготовки = {raw_model.coef_[0, 0]:6.3f}, '
          f'accuracy = {accuracy_score(y_val, pred):.3f}, '
          f'единиц предсказано {pred.sum():2d} из {len(y_val)}')

print('\nПосле StandardScaler:')
for name, X_tr, X_v in variants:
    demo_scaler = StandardScaler().fit(X_tr)
    scaled_model = LogisticRegression(C=0.05).fit(demo_scaler.transform(X_tr), y_train)
    print(f'  {name}: коэффициент подготовки = {scaled_model.coef_[0, 0]:6.3f}, accuracy = '
          f'{accuracy_score(y_val, scaled_model.predict(demo_scaler.transform(X_v))):.3f}')

print(f'\nЧто произошло. Часы превратились в сутки — значения признака уменьшились в 24 раза.')
print(f'Чтобы смысл сохранился, коэффициент должен был вырасти в 24 раза, то есть до ~18.9.')
print(f'Но регуляризация не дала: он стал не больше, а меньше — 0.239.')
print(f'Признак перестал влиять, и модель просто отвечает «класс 1» всем подряд:')
print(f'её accuracy {y_val.mean():.3f} — ровно как у тривиального ответа.')
print('StandardScaler приводит оба варианта к одинаковым числам, и разница исчезает.')
```

---

### F25 · minor · `StandardScaler` ни разу не объяснён

Ячейка 41 упоминает его как известный инструмент. В занятии 25 он был, но здесь нет даже однострочного напоминания, а раздел целиком про него.

**Fix:** добавить в ячейку 41 после первого абзаца:

```markdown
> Напоминание из занятия 25: **`StandardScaler`** вычитает из каждого признака его среднее и делит на стандартное отклонение. После этого у всех признаков среднее 0 и разброс 1 — они становятся сравнимыми, и единицы измерения перестают влиять. Обучать (`fit`) его нужно **только на train**, иначе информация из validation и test просочится в обучение.
```

---

### F26 · major · Текст п.19 противоречит собственному коду + раздел в две строки

Ячейки 43–44. Markdown говорит: «**`l1_ratio`** задаёт долю L1 в **ElasticNet**». Код же передаёт `l1_ratio=0` и `l1_ratio=1` в обычные L2- и L1-модели, где никакого ElasticNet нет. Ученик, читая текст, не поймёт, почему `LogisticRegression(C=0.05, l1_ratio=0)` подписано «сильная L2». Причина в том, что в sklearn 1.8 `penalty` объявлен устаревшим и `l1_ratio` стал **общим** параметром: 0 — чистая L2, 1 — чистая L1, промежуточные значения — ElasticNet. Код правильный и современный (проверено: у L1-варианта вес «шума» равен ровно 0.0, а не округляется до нуля) — устарел именно текст. Плюс сам раздел — две строки без напоминания, что такое L1 и L2.

**Fix:** заменить ячейку 43 целиком:

```markdown
## 19. Параметр C и сила регуляризации

Напомним из занятия 25: **регуляризация** — это добавка к функции потерь, которая штрафует модель за большие коэффициенты и тем самым мешает ей переобучиться. Два вида штрафа:

- **L2** — штраф $\sum w_j^2$: сжимает все веса, но почти никогда не делает их ровно нулевыми;
- **L1** — штраф $\sum|w_j|$: умеет обнулять веса полностью, то есть сам выбрасывает бесполезные признаки. Это называют **отбором признаков**.

В `LogisticRegression` силой штрафа управляют два параметра:

- **`C`** — **обратная** сила регуляризации: большой `C` — слабый штраф, маленький `C` — сильный. Осторожно: в занятии 25 у `Ridge` и `Lasso` был параметр `alpha`, который работает наоборот (большой `alpha` — сильный штраф);
- **`l1_ratio`** — какая доля штрафа приходится на L1. `l1_ratio=0` — чистая L2, `l1_ratio=1` — чистая L1, промежуточные значения дают смесь, которую называют **ElasticNet** («эластичная сеть»).

Оба параметра подбирают по validation.

Ниже мы добавим к двум полезным признакам третий — чистый случайный шум — и посмотрим, кто из вариантов догадается его выбросить.
```

---

### F27 · minor · Мёртвый код и повторный импорт в ячейке 44

`X_test_noise` и `X_test_scaled` вычисляются и никогда не используются; `StandardScaler` импортируется второй раз (уже был в ячейке 42). Лишние строки в учебном коде читаются как «так надо».

**Fix:** заменить начало ячейки 44 на:

```python
# Добавим к двум полезным признакам один заведомо бесполезный (случайный шум),
# чтобы увидеть, как L1 обнуляет вес ненужного признака.
noise_rng = np.random.default_rng(0)
X_train_noise = X_train.copy()
X_train_noise['шум'] = noise_rng.normal(size=len(X_train))

scaler = StandardScaler()
X_train_scaled = scaler.fit_transform(X_train_noise)
```

(остальную часть ячейки оставить как есть — она корректна, проверено: `сильная L2 [0.962 0.194 0.032]`, `слабая L2 [4.576 1.218 -0.849]`, `L1 [2.030 0.212 0.000]`, предупреждений sklearn нет).

---

### F28 · minor · Слипшийся абзац в п.20

Ячейка 45: строка «Результат всё равно оценивают выбранными метриками на отдельных данных.» приклеена к предыдущему абзацу без пустой строки, поэтому в markdown она сольётся с предыдущим предложением.

**Fix:** вставить пустую строку перед ней и заодно связать вывод с числами ниже:

```markdown
Выгоден такой обмен или нет, зависит от того, что дороже в вашей задаче — пропуск или ложная тревога.

Ниже это видно по числам: у `balanced` recall редкого класса выше, а precision — ниже. Результат всё равно оценивают выбранными метриками на отдельных данных.
```

Проверено: обычная — precision 1.000 / recall 0.800; balanced — precision 0.857 / recall 0.900. Текст ячейки 45 этим числам соответствует.

---

### F29 · major · П.21 обещает три вещи и не показывает ни одной

Ячейки 47–48. Три проблемы сразу:
1. Все три выбранных примера — уверенно класс 0 (вероятности 0.92, 0.79, 0.94, проверено). Читатель не видит ни одного случая, где «выбрать наибольшую вероятность» было бы содержательным решением.
2. Утверждение про `macro` и `weighted` не подкреплено кодом. Хуже того: iris идеально сбалансирован (50/50/50), поэтому macro и weighted F1 там **совпадают** — 0.820 и 0.820 (проверено). Любая демонстрация на iris покажет одинаковые числа, и это надо честно объяснить.
3. Слова «softmax» и «multinomial» появляются только внутри `print` в конце кодовой ячейки — то есть теория подана через вывод программы.

**Fix, часть 1** — заменить ячейку 47:

```markdown
## 21. Многоклассовая классификация *

Для трёх и более классов модель вычисляет **несколько** линейных комбинаций — по одной на класс — и превращает их в вероятности, сумма которых равна 1. Функцию, которая это делает, называют **softmax** («мягкий максимум»): она работает как сигмоида, только сразу для нескольких чисел, и отдаёт больше вероятности тому классу, у которого линейная комбинация больше. Такой режим модели называют **multinomial** («многоклассовый»). `predict` просто выбирает класс с наибольшей вероятностью.

Метрики тоже требуют уточнения: precision, recall и F1 определены для одного класса, поэтому по классам их надо как-то усреднить. `macro` даёт каждому классу одинаковый вес, `weighted` — вес, пропорциональный числу объектов класса. Если классы одинакового размера, оба способа дают одно и то же число; разница появляется только при дисбалансе.

Подробности — на следующем практическом занятии.
```

**Fix, часть 2** — заменить код ячейки 48 (числа проверены):

```python
from sklearn.datasets import load_iris

iris = load_iris()
X_iris = iris.data[:, :2]
y_iris = iris.target
multi_model = LogisticRegression(max_iter=1000).fit(X_iris, y_iris)

probabilities_iris = multi_model.predict_proba(X_iris)
show = [0, 70, 85]   # уверенный объект и два спорных
demo = pd.DataFrame({
    'P(класс 0)': probabilities_iris[show, 0],
    'P(класс 1)': probabilities_iris[show, 1],
    'P(класс 2)': probabilities_iris[show, 2],
    'сумма': probabilities_iris[show].sum(axis=1),
    'predict': multi_model.predict(X_iris[show]),
    'настоящий класс': y_iris[show],
})
print(demo.round(3).to_string(index=False))
print('\nПервая строка — уверенный прогноз. В третьей модель колеблется между')
print('классами 1 и 2 (0,38 против 0,41) и ошибается: predict берёт максимум,')
print('даже когда разрыв крошечный.\n')

predictions_iris = multi_model.predict(X_iris)
print(f'F1 по каждому классу: {np.round(f1_score(y_iris, predictions_iris, average=None), 3)}')
print(f'F1 macro    = {f1_score(y_iris, predictions_iris, average="macro"):.3f}')
print(f'F1 weighted = {f1_score(y_iris, predictions_iris, average="weighted"):.3f}')
print('\nЗдесь macro и weighted совпали — и это не совпадение: в iris ровно по 50')
print('объектов каждого класса, поэтому «равный вес» и «вес по размеру» — одно и то же.')
print('На несбалансированных данных из п.12 эти два числа разошлись бы.')
```

---

### F30 · minor · Графики в п.22 без подписей осей и легенды

Ячейка 50: у обоих `axes` есть только `set_title` и `grid`. Нет `set_xlabel`/`set_ylabel`, нет легенды, поясняющей цвет точек. Плюс accuracy печатается отдельно от картинки, хотя логичнее видеть её прямо на графике.

**Fix:** заменить блок построения графиков в конце ячейки 50:

```python
acc_plain = accuracy_score(ym_test, plain_moon.predict(moon_test))
acc_curved = accuracy_score(ym_test, curved_moon.predict(moon_test_poly))

fig, axes = plt.subplots(1, 2, figsize=(12, 4.5), sharex=True, sharey=True)
for ax, surface, title in [
    (axes[0], plain_surface, f'Прямая граница, accuracy = {acc_plain:.3f}'),
    (axes[1], curved_surface, f'После новых признаков, accuracy = {acc_curved:.3f}'),
]:
    ax.contourf(mxx1, mxx2, surface, levels=[0, 0.5, 1], cmap='coolwarm', alpha=0.25)
    ax.contour(mxx1, mxx2, surface, levels=[0.5], colors='black', linewidths=2)
    for label, colour, name in [(0, 'tab:blue', 'класс 0'), (1, 'tab:red', 'класс 1')]:
        mask = (ym_test == label)
        ax.scatter(moon_test[mask, 0], moon_test[mask, 1],
                   c=colour, edgecolor='black', s=35, label=name)
    ax.set_title(title)
    ax.set_xlabel('Признак $x_1$')
    ax.grid(alpha=0.2)
axes[0].set_ylabel('Признак $x_2$')
axes[0].legend(loc='upper right')
plt.tight_layout(); plt.show()
```

Проверено: accuracy 0.867 и 0.920.

---

### F31 · major · П.23 — самый длинный раздел без единого числа и картинки, плюс фактическая ошибка

Ячейка 51. Три проблемы:
1. **Фактическая ошибка:** «в **сквозном примере** модель училась на подготовке от 0 до 10 часов». Сквозной пример (`hours`) — это 1…8 часов; диапазон 0…10 относится к синтетическому двумерному набору, а его train — 0.2…9.9 ч (проверено).
2. Раздел утверждает, что за границами train прогнозы ненадёжны, но ничего не показывает. Между тем это идеально демонстрируется: модель при 100 часах подготовки выдаёт P(сдал) = **1.000000** (проверено) — уверенность, которую нечем подтвердить.
3. «вероятности **логрег**» — нерасшифрованный жаргон, единственное место в блокноте.
4. Заголовок объединяет два независимых понятия через «и».

**Fix, часть 1** — заменить ячейку 51 целиком (номер не меняем):

```markdown
## 23. Границы, за которые нельзя выходить

**Ограничения интерпретации.** Коэффициенты логистической регрессии читаются удобно, но у такого чтения есть границы:

- коэффициент показывает связь, а не причину: признак может просто идти «в комплекте» с настоящей причиной;
- коэффициент верен только при неизменных остальных признаках, а в жизни признаки часто меняются вместе;
- если признаки сильно связаны между собой, модель может почти произвольно поделить вклад между ними, и знак отдельного коэффициента становится неустойчивым;
- сравнивать коэффициенты друг с другом можно только после приведения признаков к одному масштабу (п. 18);
- одинаковый прирост $z$ по-разному меняет вероятность в середине и у краёв диапазона (п. 17).

**Ограничения экстраполяции.** **Экстраполяция** — это применение модели к таким значениям признаков, которых в обучающих данных не было: за пределами их диапазона. Прогноз внутри диапазона обучающих данных называют **интерполяцией**.

Коэффициенты и вероятности логистической регрессии описывают связь **внутри диапазона обучающих данных**. Наш двумерный пример учился на подготовке примерно от 0 до 10 часов. Формула сигмоиды формально выдаст ответ и для 100 часов — и выдаст его максимально уверенно. Но проверить его не на чем: таких учеников модель не видела, и связь там могла бы вести себя совсем иначе. Посмотрим на это в числах.
```

**Fix, часть 2** — вставить **новую кодовую ячейку сразу после ячейки 51** (все числа проверены):

```python
low, high = X_train['подготовка'].min(), X_train['подготовка'].max()
print(f'Модель видела подготовку от {low:.1f} до {high:.1f} часов.\n')

probe = pd.DataFrame({'подготовка': [2, 5, 8, 10, 20, 50, 100], 'сон': 7.0})
probe['P(сдал)'] = model.predict_proba(probe)[:, 1]
probe['это'] = np.where(probe['подготовка'] <= high, 'интерполяция', 'ЭКСТРАПОЛЯЦИЯ')
print(probe.round(6).to_string(index=False))

grid_hours = np.linspace(0, 100, 400)
curve = model.predict_proba(pd.DataFrame({'подготовка': grid_hours, 'сон': 7.0}))[:, 1]

plt.figure(figsize=(8, 4))
plt.plot(grid_hours, curve, color='navy', linewidth=2, label='P(сдал) при сне 7 ч')
plt.axvspan(low, high, color='tab:green', alpha=0.15, label='здесь модель училась')
plt.xlabel('Часы подготовки'); plt.ylabel('Вероятность сдать')
plt.title('За пределами train модель уверена, но проверить её нечем')
plt.ylim(-0.05, 1.05); plt.grid(alpha=0.3); plt.legend(); plt.show()

print('\nПри 100 часах подготовки модель обещает P = 1.000000 — абсолютную уверенность.')
print('Это не знание, а продолжение формулы: ни одного такого ученика в данных не было.')
```

---

### F32 · major · Число печатается и нигде не объясняется; калибровочная кривая слишком шумная

Ячейки 52–53. Код печатает «Brier score константы: 0.230» (проверено), но markdown про эту константу не говорит ни слова — читатель видит число без смысла. Кроме того, `calibration_curve(..., n_bins=5)` на 50 тестовых объектах даёт по ~10 объектов на точку, и «реальная доля» скачет 0.0 / 0.3 / 0.9 / 1.0 / 1.0 (проверено) — по такой ломаной делать выводы о калибровке нельзя, и об этом не предупреждено.

**Fix, часть 1** — добавить абзац в конец ячейки 52:

```markdown
Чтобы понять, много это или мало, Brier score всегда сравнивают с чем-то. Наш ориентир — «модель», которая всем подряд называет одну и ту же вероятность, равную доле класса 1 в train. Если наша модель не обгоняет такую константу, вероятности она предсказывать не умеет.

И сразу оговорка про картинку: в test всего 50 объектов, а точек на калибровочной кривой пять — значит, каждая точка посчитана примерно по 10 объектам. Такая кривая сильно скачет, и по ней видно только грубую тенденцию, а не точную картину. На настоящих задачах для калибровочной кривой берут тысячи объектов.
```

**Fix, часть 2** — заменить хвост кода ячейки 53:

```python
print(f'Brier score модели:    {brier_score_loss(y_test, p_test):.3f} (меньше — лучше)')
baseline_probability = np.full_like(p_test, y_train.mean())
print(f'Brier score константы: {brier_score_loss(y_test, baseline_probability):.3f} '
      f'(всем подряд говорим P = {y_train.mean():.2f})')
print(f'\nМодель в {brier_score_loss(y_test, baseline_probability) / brier_score_loss(y_test, p_test):.1f} '
      'раза лучше константы — её вероятности несут информацию.')
```

Проверено: 0.041 против 0.230, отношение 5.6.

---

### F33 · major · Изотоническая кривая строится по трём точкам; вывод раздела спрятан в `print`

Ячейка 54. `calibration_curve` для isotonic возвращает не 5, а **3** точки (проверено): изотоническая регрессия склеивает вероятности в несколько одинаковых значений, и квантильные корзины схлопываются. На графике это ломаная из двух отрезков рядом с пятиточечными линиями — визуально она выглядит «прямее», хотя это артефакт, а не хорошая калибровка. При этом главный вывод раздела («Brier не улучшился, и это ожидаемо») напечатан четырьмя строками `print`, то есть теория подана как вывод программы.

**Fix, часть 1** — в ячейке 54 заменить блок построения на такой, где число точек честно видно, и убрать теорию из `print`:

```python
print('Brier score на test (меньше — лучше):')
for probabilities, label in [(p_test, 'без калибровки'),
                             (p_test_platt, 'Platt'),
                             (p_test_isotonic, 'isotonic')]:
    observed_c, predicted_c = calibration_curve(y_test, probabilities,
                                                n_bins=5, strategy='quantile')
    print(f'  {label:<16}: Brier = {brier_score_loss(y_test, probabilities):.3f}, '
          f'точек на кривой: {len(observed_c)}')

plt.figure(figsize=(6, 5))
plt.plot([0, 1], [0, 1], '--', color='gray', label='идеальная калибровка')
for probabilities, label in [
    (p_test, 'без калибровки'),
    (p_test_platt, "Platt (method='sigmoid')"),
    (p_test_isotonic, "isotonic (method='isotonic')"),
]:
    observed_c, predicted_c = calibration_curve(y_test, probabilities,
                                                n_bins=5, strategy='quantile')
    plt.plot(predicted_c, observed_c, marker='o',
             label=f'{label}, Brier={brier_score_loss(y_test, probabilities):.3f}')
plt.xlabel('Средняя предсказанная вероятность')
plt.ylabel('Реальная доля класса 1')
plt.title('Калибровка настроена на validation, проверена на test')
plt.grid(alpha=0.3); plt.legend(); plt.show()
```

**Fix, часть 2** — вынести вывод в **новую markdown-ячейку сразу после ячейки 54**:

```markdown
Что мы увидели. Brier score после калибровки не улучшился: 0,041 без неё, 0,056 у Platt и 0,042 у изотонической. И это ожидаемо. Логистическая регрессия обучается минимизацией log loss, то есть **сама** стремится выдавать честные вероятности — калибровать здесь почти нечего. А калибратор, настроенный всего на 38 валидационных объектах, добавляет больше шума, чем пользы.

Заметьте ещё одну деталь: у изотонической калибровки на графике всего три точки, а не пять. Изотоническая регрессия — ступенчатая функция, она склеивает многие вероятности в одинаковые значения, и корзины схлопываются. На маленькой выборке это ещё один признак того, что ей не хватает данных.

Вывод: калибровка окупается тогда, когда вероятности модели заметно смещены — например, у деревьев и случайного леса, о которых пойдёт речь дальше в курсе. Для логистической регрессии её обычно не требуется.
```

Проверено: Brier 0.041 / 0.056 / 0.042; число точек 5 / 5 / 3.

---

### F34 · minor · Правило «test открывают один раз» нарушается самим блокнотом

Ячейки 36, 38, 53, 54. Блокнот четырежды повторяет принцип «test открывают **один раз**» (ячейки 22, 34, 35, 55) и при этом обращается к test в четырёх разных местах: финальные метрики, график границы решений, калибровочная кривая, сравнение калибраторов. Внимательный ученик увидит противоречие.

**Fix:** после переноса test-отчёта в п.14 (F17) и замены точек на train в п.16 (F22) остаётся п.24. Добавить оговорку в конец ячейки 52:

```markdown
> Небольшая честная оговорка. Мы уже открывали test в разделе 14 и открываем его снова здесь. Правило «один раз» относится к **принятию решений**: нельзя, посмотрев на test, вернуться и поменять модель или порог. Смотреть на уже зафиксированную модель с разных сторон — метрики, кривые, калибровка — можно, если по итогам этого мы ничего не переделываем. Именно так мы и поступим: ниже мы только смотрим и ничего не меняем.
```

---

### F35 · minor · В итоговом чек-листе п.25 нет ROC/PR и калибровки

Ячейка 55. Порядок работы из 10 пунктов пропускает две вещи, которым посвящены целые разделы: оценку ранжирования (п.15) и калибровку (п.24). Плюс лишняя пустая строка после заголовка.

**Fix:** заменить пункты 8–10 ячейки 55 (и убрать лишнюю пустую строку после `## 25. …`):

```markdown
8. При необходимости применить масштабирование, регуляризацию или веса классов — снова обучая преобразования только на train.
9. Проверить качество ранжирования по ROC- и PR-кривым, а если нужны сами вероятности — их калибровку.
10. **Один раз** оценить финальный вариант на test.
11. Зафиксировать ограничения (диапазон данных, границы интерпретации) и следить за качеством после внедрения.
```

---

## 3. Общие наблюдения

**1. Главная системная проблема — «сказано, но не показано».** Раздел формулирует правильный тезис, а код рядом печатает метрику, из которой этот тезис не следует. Так устроены п.6 (сходимость), п.15 (ROC против PR при дисбалансе), п.17 (интерпретация коэффициентов), п.18 (механизм регуляризации), п.21 (усреднение метрик), п.23 (экстраполяция). Это ровно шесть из десяти теоретически насыщенных разделов, и во всех шести не хватает 5–15 строк кода, а не переписывания текста.

**2. «Разделы в одну строчку».** Формально коротких, где нечего разбирать: п.14 (две фразы, ноль чисел — при том что это кульминация блокнота), п.19 (две фразы про `C` перед довольно сложным кодом), п.23 (длинный текст, но ноль демонстраций). Пограничные: п.12 (дублирует п.11), п.21 и п.22 оба заканчиваются одинаковой фразой «Подробности — на следующем практическом занятии», что читается как «а мы не будем объяснять».

**3. Графики.** Из 14 графиков подписи осей отсутствуют у двух (ячейка 50), сетка — у одного (ячейка 38), легенда для точек — у двух (ячейки 38, 50). Терминологического расхождения «текст говорит FPR/TPR — на осях написано другое» (ячейка 36) достаточно, чтобы ученик потерял связь между разделом и картинкой.

**4. Терминология.** Расшифровка терминов сделана в целом хорошо — TP/FP/FN/TN, odds, logit, экстраполяция, Brier, FrozenEstimator разобраны образцово. Провалы сосредоточены в двух местах: (а) понятия из занятия 25 (L1, L2, Ridge, Lasso, ElasticNet, `StandardScaler`) используются без единого слова напоминания, хотя между занятиями два урока; (б) четыре термина введены через `print` внутри кодовой ячейки (softmax, multinomial, наблюдение про L1) вместо markdown.

**5. Технически код в порядке.** Устаревших API нет: `l1_ratio` вместо `penalty`, `FrozenEstimator` вместо `cv='prefit'` — всё соответствует sklearn 1.8. Прогон не даёт ни одного `FutureWarning` или `UserWarning` (проверено). Все числа в markdown, которые я смог сопоставить с выводом, совпадают. Единственная фактическая ошибка в числах — «сквозной пример … от 0 до 10 часов» (F31), единственная неверная ссылка — «раздел 13» (F11).

**6. Мелкая редактура.** По всему блокноту дефис вместо тире («Вероятность - это число», «Подробности - на следующем занятии», «AUC - Area Under Curve» — ячейки 4, 6, 34, 35, 47, 49). Пропущенные «ё» в новых кусках («вернемся», «ее», «жестко», «Несложно») при том, что остальной текст «ё» соблюдает. Три опечатки: «функций log» (6), «Получившая функция» (8), «к нашей задаче определение вероятности» (10). Повторные импорты `DummyClassifier` (31) и `StandardScaler` (44).

**7. Заголовки против правила одного понятия.** Нарушают: п.6 («log loss и оптимизация»), п.12 («Дисбаланс классов и выбор метрики»), п.23 («Ограничения интерпретации и экстраполяции»). Пограничный: п.16 («Несколько признаков и граница решений») — здесь можно переименовать в `## 16. Граница решений при нескольких признаках`. Не нарушают, хотя формально содержат «и»: п.3 (логит и сигмоида — взаимно обратные функции, одно понятие), п.8 (train/validation/test — одно разбиение), п.15 (ROC и PR — один вопрос «как оценить ранжирование»).

---

## 4. Verdict

**Needs substantial rework — но рework содержательный, а не структурный.**

Обоснование. Каркас блокнота правильный: последовательность разделов логична, сквозной пример удачный, код современный и работает без предупреждений, объяснение odds → logit → сигмоида в п.3 — лучшее место всего материала. Переписывать архитектуру не нужно, переименовывать разделы не нужно.

Однако «light touch-ups» это не по трём причинам. Во-первых, есть два блокера: неверная навигационная ссылка (F11) и раздел-кульминация п.14, в котором физически нет финальной проверки на test (F17) — ученик, идущий по инструкции, не найдёт того, что ему обещали. Во-вторых, шесть разделов утверждают тезис, который их собственный код не подтверждает; в двух случаях (п.15 про дисбаланс, п.18 про механизм регуляризации) это именно то, ради чего раздел написан. В-третьих, п.17 упускает единственную точную интерпретацию коэффициентов логистической регрессии — через отношение шансов $e^{w}$ — хотя весь необходимый аппарат построен в п.3 сорока строками выше. Это не «можно улучшить», это отсутствующий кусок теории.

Объём работы: примерно 12 замен markdown, 8 замен кодовых ячеек и 4 новые ячейки. Ни одна правка не затрагивает нумерацию разделов, поэтому упражнения, практика, CHANGELOG, STUDENT_REVIEW и карта понятий остаются нетронутыми.

---

## 5. TOP-5 изменений

1. **F17 — вернуть п.14 его содержание.** Вставить кодовую ячейку с финальным отчётом (порог 0.35, validation 0.895/0.862/1.000/0.926 против test 0.960/0.941/1.000/0.970, проверено) и убрать эти строки из ячейки 36. Это чинит кульминацию блокнота и заодно делает корректной ссылку из F11.

2. **F23 — добавить в п.17 отношение шансов.** Один абзац про $e^{w_j}$ плюс столбец в таблице коэффициентов: подготовка умножает шансы на 3.7, сон — на 1.6 (проверено). Единственная точная интерпретация коэффициентов, и она замыкает круг с п.3.

3. **F18 — показать, а не объявить, разницу ROC и PR при дисбалансе.** Новая ячейка после 36: ROC-AUC 0.861 выглядит прилично, но чтобы поймать 80% редкого класса, precision падает до 0.131 (проверено). Сейчас центральный тезис раздела держится только на строчке в таблице.

4. **F24 — напечатать коэффициенты в п.18.** Показать, что в «сутках» коэффициент стал 0.239 вместо необходимых ~18.9, и модель скатилась к ответу «класс 1 всем» — 38 единиц из 38 (проверено). Это превращает абстрактное «регуляризация бьёт по полезному признаку» в наблюдаемый факт.

5. **F11 — исправить ссылку «в разделе 13» на «в разделе 14».** Одно слово, но сейчас оно отправляет ученика не в тот раздел в самом принципиальном месте курса — правиле обращения с тестовой выборкой.

The git status is clean with Lesson 27 untouched. Now I'm structuring a comprehensive report in Russian, mapping out the notebook cells: starting with a header and first section, then loading data and creating a scatter plot, followed by linear regression analysis, a log-odds visualization, logistic/sigmoid formulas, and finally the sigmoid function itself.

Continuing through the remaining cells, I'm planning sections on linear combinations, decision thresholds, probability tables, log loss plotting and formulas, gradient descent implementation, then moving into 2D data generation, train-test splitting, confusion matrices, and performance metrics. The report will cover baseline models, class imbalance handling, threshold tables, ROC/PR curves on test data, decision boundaries in 2D space, model coefficients, feature scaling, L1/L2 regularization, balanced class weights, and finally the iris and moons datasets with calibration curves using CalibratedClassifierCV.

I've also noted a key finding: in cell 4 (section 2), the linear regression line extends beyond the valid range, which the plot demonstrates clearly at the boundaries.

For cell 6 (section 3), there's a typo in the phrasing about using the log function, and the notation doesn't clarify that we're using the natural logarithm. This inconsistency with ln versus log notation appears across multiple cells (6, 7, 8, 16, 18), so I should consolidate this into one finding. Additionally, there's no numerical walkthrough showing the round-trip conversion from probability to odds to logit and back to probability, which would help clarify the relationships. F6 через F10 выявляют дополнительные проблемы: отсутствие пояснений для ключевых терминов (квазиньютоновский метод, бинарная кросс-энтропия), несогласованность в обозначениях логарифмов, недостаток визуализации сходимости градиентного спуска и неполные объяснения в ячейках про пороги и синтетические данные.

F11 через F15 указывают на ошибки в перекрёстных ссылках, отсутствие контекста о размерах валидационного и тестового наборов, пропущенные английские эквиваленты терминов и неясные определения метрик вроде precision и recall.

F16 выявляет дублирование раздела 12 с 11, отсутствие пояснения об искусственности данных из make_classification и недостаток примеров выбора метрики по стоимости ошибок.

F17 отмечает, что раздел 14 слишком краток и не содержит числовых данных, а финальная проверка на тестовом наборе фактически происходит позже в разделе 15.

F18 и F19 указывают на то, что ключевое утверждение о преимуществах PR-кривой при дисбалансе классов не подкреплено примерами, а аббревиатуры TPR, FPR, AP и ROC не расшифрованы.

F20 начинает отмечать проблему с подписями осей на графике ROC.

Продолжая проверку, вижу, что подписи осей не совпадают с текстом, метрики на тестовом наборе выше валидационных без объяснения, а ROC-кривая построена всего на 50 объектах, что дает только 6 точек.

F21 и F22 указывают на отсутствие расшифровки термина «гиперплоскость» и недостаток сетки и легенды на графике.

F23 выявляет, что раздел 17 не связывает коэффициенты логистической регрессии с шансами через экспоненту, хотя odds были введены ранее, и таблица коэффициентов не интерпретируется.

F24 отмечает, что механизм влияния регуляризации на коэффициенты упоминается, но сами коэффициенты не выводятся на печать для проверки.

F25 указывает, что StandardScaler никогда не объясняется.

F26 показывает противоречие в описании l1_ratio в ElasticNet — в коде используется l1_ratio=0, что соответствует L2, а не смешанной регуляризации, и раздел 19 занимает всего две строки.

F27 отмечает неиспользуемые переменные X_test_noise и X_test_scaled, а также повторный импорт StandardScaler.

F28 указывает на слипшийся абзац в ячейке 45.

F29 выявляет, что в разделе 21 заявлено усреднение macro и weighted метрик, но оно не показано; на iris они совпадают; все три примера из класса 0, что не демонстрирует различия; softmax и multinomial объявляются только через print.

F30 отмечает проблему с графиком в ячейках 49/50.

Графики без подписей осей и легенды; раздел 23 не содержит ни одного числа и графика, упоминание о временном диапазоне от 0 до 10 часов неверно для сквозного примера, используется жаргон «логрег»; Brier score константы печатаются без объяснения, калибровочная кривая построена на 50 объектах с 10 в каждой корзине; isotonic-кривая рисуется по 3 точкам, вывод в print вместо markdown, раздел 24 нарушает правило использования test один раз; в ячейке 55 лишняя пустая строка и отсутствует пункт про калибровку в разделе 25.

Есть также сквозные проблемы: неправильное использование тире и дефисов, буква ё, повторяющаяся фраза про следующее практическое занятие, повторные импорты, и test используется 4 раза вместо одного. Проверяю F11 — в ячейке 22 сказано, что test впервые используется в разделе 13, но на самом деле это происходит в ячейке 36 под разделом про ROC и PR-кривые. Я вижу, что ячейка 36 переиспользует вычисления из новой ячейки п.14, поэтому нужно либо удалить дублирующиеся строки, либо переписать её полностью. Записываю проверенные метрики для п.14: порог 0.35, валидация (accuracy 0.895, precision 0.862, recall 1.000, F1 0.926), тест (accuracy 0.960, precision 0.941, recall 1.000, F1 0.970) с ROC-AUC 0.993 и AP 0.996. Теперь переходу к исправлению F18 для сравнения ROC и PR кривых. units; after scaling both reach 0.965 / 0.868. The hourly coefficient of 0.786 scales to about 19 when converted to daily, which is worth noting for consistency. F29 shows per-class F1 scores of 1.0, 0.733, and 0.727 with macro and weighted averages both at 0.820, distributed across 0, 70, and 85 objects. F31 reaches probability 1.0 at 100 hours with training data spanning 0.2 to 9.9. For F9, gradient descent yields w=0.794 and b=-4.163 reducing log loss from 0.6931 to 0.3491, while sklearn with C=10000 gives w=1.134 and b=-6.114.

Now I'm refining the section titles. Section 6 should focus on how the model learns through log loss rather than listing optimization as a separate concern—simplifying to just "Как обучается модель" captures the core concept. Section 12 needs to emphasize why accuracy fails with imbalanced classes. Section 16 should center on decision boundaries with multiple features. Section 23 appears to cover two distinct ideas that might need separation.

I'll keep sections 2, 3, 15, and 8 as they are since each represents a single coherent concept. The report will be comprehensive and thorough, even if lengthy, since that's what's needed here.
