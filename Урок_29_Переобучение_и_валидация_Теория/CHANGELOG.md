# CHANGELOG — Урок_29_Переобучение_и_валидация_Теория

## Глубокая переработка по итогам ревью (все находки F1–F45)

### Протокол данных
- Split стал **трёхчастным**: сначала откладывается test (20 %), остаток делится на train/validation — 100 / 44 / 36. Раньше test обещали в шапке и в п. 6, но никогда не создавали и не открывали.
- **П. 16** доведён до конца: перебор степеней по CV, переобучение на train+validation, единственное измерение на test — MSE 2.23 при уровне шума 2.25.
- Кросс-валидация (п. 10, п. 16) считается на train+validation, а не на всей таблице: отложенный test не участвует нигде.
- Все числа в тексте пересчитаны под новый split и сверены с фактическим выводом ячеек.

### Новые демонстрации вместо утверждений
- **П. 1:** код с train/validation error и сравнением с прогнозом «всегда среднее» (10.11).
- **П. 3:** график «полином 15-й степени на 30 и на 100 точках» — train 0.93 против val 27.17.
- **П. 5:** код печатает `degree` (задали мы) против `coef_`/`intercept_` (выучила модель).
- **П. 6:** схема-диаграмма трёх ролей одной таблицы.
- **П. 7:** эксперимент на 20 разбиениях — степень 2 побеждает лишь 10 раз из 20, MSE степени 2 гуляет от 1.08 до 2.59.
- **П. 10:** столбчатая диаграмма пяти fold (1.31…2.21) — разброс перестал быть словами.
- **П. 11:** демонстрация стратификации — доли редкого класса 0.75/0/0 против 0.25/0.25/0.25.
- **П. 12:** утечка на числах — отбор признаков из чистого шума даёт «невозможные» 0.673 против честных 1.110.
- **П. 14:** схема вложенной CV (внешний test / внутренняя validation / train).
- **П. 15:** код печатает версии Python, numpy, sklearn.

### Исправленные ошибки
- **П. 12:** утверждение «`poly.fit_transform` до split — утечка» было неверным: `PolynomialFeatures` не считает статистик по данным. Заменено на список шагов, которые их действительно считают (`StandardScaler`, `SimpleImputer`, `SelectKBest`, `PCA`).
- **П. 9:** доли train в тексте (25 %, 40 %) не совпадали с кодом (20 %…100 %); кривая строилась по одной подвыборке и была дёрганой — теперь усреднение по 5 запускам.
- **П. 13:** повтор правила из п. 12 убран; добавлена оговорка, что утечка масштаба для линейной регрессии может не изменить результат, но правило соблюдают всегда.
- **П. 18 (схемы split):** нумерация запусков CV начиналась с 0.

### Термины и подача
- Расшифрованы при первом употреблении: MSE, CV, data leakage, стратификация, grid search, nested CV, `neg_mean_squared_error` (соглашение sklearn «больше — лучше»).
- В шапке объяснено, что делает `PolynomialFeatures`, и что означает `*` у заголовка раздела.
- Введён сквозной мотив «уровень шума 2.25» — пол, ниже которого не опустится никакая модель; он отмечен на трёх графиках и закрывает занятие в п. 16.
- **П. 8:** validation curve разделена на две панели (общий вид и крупный план), иначе всё, кроме степени 1, сливалось в полоску.
- Заголовки без склейки двух понятий: п. 11 → «Схема разбиения должна повторять структуру данных», п. 13 → «Утечки при переборе гиперпараметров», п. 15 → «Воспроизводимость результата». Номера разделов сохранены.
- После каждого нового графика добавлен разбор «что именно здесь видно».

### Проверка
- Все 46 ячеек выполняются целиком без ошибок (Python 3.14, sklearn 1.8.0).
- Каждое число, названное в markdown, сверено с фактическим выводом соответствующей ячейки.

## Новый п. 15 «Воспроизводимость»

- Добавлен раздел: seed и фиксация случайности (данные, split, CV — почему всюду 42), правила сравнения идей при одинаковом seed, таблица версий артефактов (данные, код, окружение, модель).
- Финальный протокол стал п. 16; в шаг 6 добавлены seed и версии артефактов.
- Обновлены ссылки: упражнение 10 (п. 15 → п. 16), LESSON_OUTLINE (16 разделов).
- Canvas `ml-theory-concepts-map`: занятие 29 — 16 разделов, строка «Воспроизводимость», миграция переименованных плашек.

## CV только после определения (п. 10)

- **П. 7:** убраны «CV» и «fold» до введения термина; вместо этого — «повторить split несколько раз».
- **Шапка:** из целей убрано «k-fold CV».
- **П. 9 (learning curve):** код переписан без `KFold`/`learning_curve` — validation на отложенной выборке, пока train растёт; CV впервые появляется в п. 10.

## Читаемость: убраны перекрёстные ссылки, переписан п. 4

- Удалены почти все ссылки вида «(п. N)» и «(занятие NN, п. N)» — текст читается без прыжков; оставлена одна ссылка вперёд (learning curve → «кросс-валидация — следующий раздел») как необходимая.
- Убраны ссылки на будущее (занятие 30, пп. 8–15 из ранних разделов); формулировки заменены самодостаточными.
- **П. 4 (смещение и разброс)** переписан: объяснение «двух источников ошибки» словами на сквозном примере + новая code-ячейка — 5 обучений на случайных подвыборках train для степеней 1 и 15 (прямые совпадают и промахиваются vs кривые гуляют).

## Сквозной пример через весь блокнот (точечные правки)

- Code-ячейка с датасетом и split 70/30 перенесена из п. 6 в шапку занятия — сквозной пример вводится сразу.
- **П. 1:** добавлена привязка train/validation error к синим/оранжевым точкам сквозного графика.
- **П. 2:** таблица «условных чисел» заменена реальными MSE сквозного примера (степени 1/2/15); добавлена code-ячейка с тремя кривыми и train/val MSE в легенде.
- **П. 6:** отмечено, что split уже сделан в начале занятия.
- **П. 15:** финальный протокол проиллюстрирован шагами на сквозном примере.
- Правки внесены точечно в блокнот (без перегенерации скриптом).

## Полная переработка теории (занятие 29)

### Структура
- **15 разделов** вместо 17 куцых: порядок «ошибка → три состояния → split → кривые → CV → утечки → протокол».
- Введены **цели занятия** и сквозной пример в шапке.
- **П. 1:** определения train/validation error; MSE — ссылкой на занятие 25 (пп. 4 и 6), без повтора формулы.
- **П. 2:** таблица трёх состояний + числовой пример + связь со степенью полинома.
- **П. 3:** утечка — ссылкой на занятие 21 (п. 10), без повторного определения.
- **П. 4:** bias–variance с таблицей на $y \approx x^2$.
- **П. 5:** таблица параметр vs гиперпараметр на сквозном примере.
- **П. 6:** train/val/test — напоминание со ссылкой на занятие 21 (п. 4); новое — «обучение на test»; code: датасет, scatter, `train_test_split`.
- **Пп. 11–12:** независимость объектов и «fit только на train» — ссылки на занятие 21 (пп. 9, 10, 13); новое — инструменты CV и fit внутри каждого fold.
- **Пп. 8–11:** validation curve, learning curve, k-fold, схемы split — с инструкциями «как читать».
- **П. 12:** правило fit внутри fold + пример `PolynomialFeatures`.
- **П. 13:** grid search и типичные утечки.

### Синхронизация
- Упражнения: **10 задач** (было 8); добавлены п. 1 (validation error) и п. 6 (test один раз).
- Практика: ссылки п. 6–10, пп. 1–2 для терминов; авторские ответы и подробные критерии перенесены в основной `practice`.
- Cross-lesson: `Урок_35_Boosting_Теория` п. 11 — без изменений.

### Скрипты
- `scripts/rebuild_overfitting_validation_theory.py`
- `scripts/rebuild_overfitting_validation_exercises.py`

### Проверка
- 5 code-ячеек теории выполняются headless (matplotlib Agg).
- `Урок_30_Переобучение_и_валидация_Практика.ipynb` — выполняется.
