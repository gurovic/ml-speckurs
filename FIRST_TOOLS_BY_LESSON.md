# Первое появление инструментов по урокам

| Урок | Тема | Какие инструменты появляются впервые |
|---:|---|---|
| 1 | Знакомство с Colab, NumPy и Pandas | `np.array`, `pd.Series`, `pd.DataFrame`, `.head()`, `.shape`, `.columns`, `.dtypes` |
| 2 | Практика: NumPy, скорость и первый анализ данных | `pd.read_csv`, `np.arange`, `.info()`, `.describe()`, `.loc`, `.iloc` |
| 3 | Фильтрация данных и обработка пропусков | `.query()`, `.isin()`, `.isna()`, `.notna()`, `.fillna()`, `.dropna()`, `.copy()` |
| 4 | Практика: фильтрация, пропуски и первая модель | `.value_counts()`, `LinearRegression` |
| 5 | Кастомная трансформация и объединение данных | `.apply()`, `.str`, `.merge()`, `pd.concat`, `.drop_duplicates()` |
| 6 | Практика: кастомная трансформация и объединение данных | `.sample()` |
| 7 | Работа с датами и агрегации | `pd.to_datetime`, `.dt`, `.astype()`, `.groupby()`, `.agg()`, `.pivot_table()`, `.sort_values()`, `.reset_index()` |
| 8 | Практика: даты и агрегации | — |
| 9 | Визуализация данных | `matplotlib.pyplot`, `plt.figure`, `seaborn`, `sns`, `histplot`, `heatmap` |
| 10 | Практика: визуализация данных | — |
| 11 | Описательная статистика и распределения | `np.random` |
| 12 | Практика: анализ формы и плотности | `np.log1p`, `np.sqrt` |
| 13 | Кластеризация | `KMeans`, `plt.plot`, `plt.scatter`, `plt.show` |
| 14 | Практика: кластеризация | `StandardScaler` |
| 15 | Разброс, аномалии и проверка гипотез | `scipy.stats`, `ttest`, `mannwhitney`, `PCA`, `boxplot`, `violinplot` |
| 16 | Практика: разброс, аномалии и гипотезы | — |
| 21 | ML workflow и kNN | `train_test_split`, `DummyClassifier`, `KNeighborsClassifier`, `KNeighborsRegressor`, `accuracy_score`, `recall_score`, `confusion_matrix`, `np.where` |
| 22 | Практика: kNN | `plt.bar`, `plt.boxplot`, `plt.imshow` |
| 23 | Feature Engineering | `OneHotEncoder`, `OrdinalEncoder`, `Pipeline`, `mean_absolute_error`, `np.linspace` |
| 24 | Практика: Feature Engineering | `.map()`, `np.clip`, `pd.to_timedelta` |
| 25 | Линейная регрессия | `Ridge`, `Lasso`, `PolynomialFeatures`, `mean_squared_error`, `r2_score`, `.corr()` |
| 26 | Практика: линейная регрессия | `ColumnTransformer` |
| 27 | Логистическая регрессия | `LogisticRegression`, `precision_score`, `f1_score`, `ConfusionMatrixDisplay` |
| 28 | Практика: логистическая регрессия | — |
| 29 | Переобучение и валидация | `GridSearchCV`, `KFold`, `cross_validate` |
| 30 | Практика: переобучение и validation curve | `learning_curve` |
| 31 | Решающее дерево | `DecisionTreeClassifier`, `DecisionTreeRegressor`, `plot_tree`, `permutation_importance` |
| 32 | Практика: решающее дерево | — |
| 33 | Bagging и случайный лес | `BaggingClassifier`, `RandomForestClassifier`, `RandomForestRegressor`, `GradientBoostingClassifier`, `.unique()`, `plt.hist` |
| 34 | Практика: bagging и случайный лес | — |
| 35 | Градиентный бустинг | `GradientBoostingRegressor` |
| 36 | Практика: бустинг в библиотеках | `DummyRegressor`, `HistGradientBoostingRegressor`, `XGBRegressor`, `LGBMRegressor`, `CatBoostRegressor`, `.set_index()` |
