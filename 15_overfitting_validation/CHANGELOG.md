# CHANGELOG — 15_overfitting_validation

## Новый п. 15 «Воспроизводимость»

- Добавлен раздел: seed и фиксация случайности (данные, split, CV — почему всюду 42), правила сравнения идей при одинаковом seed, таблица версий артефактов (данные, код, окружение, модель).
- Финальный протокол стал п. 16; в шаг 6 добавлены seed и версии артефактов.
- Обновлены ссылки: упражнение 10 (п. 15 → п. 16), LESSON_OUTLINE (16 разделов).
- Canvas `ml-theory-concepts-map`: занятие 29 — 16 разделов, строка «Воспроизводимость», миграция переименованных плашек.

## CV только после определения (п. 10)

- **П. 7:** убраны «CV» и «fold» до введения термина; вместо этого — «повторить split несколько раз».
- **Шапка:** из целей убрано «k-fold CV».
- **П. 9 (learning curve):** код переписан без `KFold`/`learning_curve` — validation на отложенной выборке, пока train растёт; CV впервые появляется в п. 10.

## Читаемость: убраны перекрёстные ссылки, переписан п. 4

- Удалены почти все ссылки вида «(п. N)» и «(занятие NN, п. N)» — текст читается без прыжков; оставлена одна ссылка вперёд (learning curve → «кросс-валидация — следующий раздел») как необходимая.
- Убраны ссылки на будущее (занятие 30, пп. 8–15 из ранних разделов); формулировки заменены самодостаточными.
- **П. 4 (смещение и разброс)** переписан: объяснение «двух источников ошибки» словами на сквозном примере + новая code-ячейка — 5 обучений на случайных подвыборках train для степеней 1 и 15 (прямые совпадают и промахиваются vs кривые гуляют).

## Сквозной пример через весь блокнот (точечные правки)

- Code-ячейка с датасетом и split 70/30 перенесена из п. 6 в шапку занятия — сквозной пример вводится сразу.
- **П. 1:** добавлена привязка train/validation error к синим/оранжевым точкам сквозного графика.
- **П. 2:** таблица «условных чисел» заменена реальными MSE сквозного примера (степени 1/2/15); добавлена code-ячейка с тремя кривыми и train/val MSE в легенде.
- **П. 6:** отмечено, что split уже сделан в начале занятия.
- **П. 15:** финальный протокол проиллюстрирован шагами на сквозном примере.
- Правки внесены точечно в блокнот (без перегенерации скриптом).

## Полная переработка теории (занятие 29)

### Структура
- **15 разделов** вместо 17 куцых: порядок «ошибка → три состояния → split → кривые → CV → утечки → протокол».
- Введены **цели занятия** и сквозной пример в шапке.
- **П. 1:** определения train/validation error; MSE — ссылкой на занятие 25 (пп. 4 и 6), без повтора формулы.
- **П. 2:** таблица трёх состояний + числовой пример + связь со степенью полинома.
- **П. 3:** утечка — ссылкой на занятие 21 (п. 10), без повторного определения.
- **П. 4:** bias–variance с таблицей на $y \approx x^2$.
- **П. 5:** таблица параметр vs гиперпараметр на сквозном примере.
- **П. 6:** train/val/test — напоминание со ссылкой на занятие 21 (п. 4); новое — «обучение на test»; code: датасет, scatter, `train_test_split`.
- **Пп. 11–12:** независимость объектов и «fit только на train» — ссылки на занятие 21 (пп. 9, 10, 13); новое — инструменты CV и fit внутри каждого fold.
- **Пп. 8–11:** validation curve, learning curve, k-fold, схемы split — с инструкциями «как читать».
- **П. 12:** правило fit внутри fold + пример `PolynomialFeatures`.
- **П. 13:** grid search и типичные утечки.

### Синхронизация
- Упражнения: **10 задач** (было 8); добавлены п. 1 (validation error) и п. 6 (test один раз).
- Практика/solution: ссылки п. 6–10, пп. 1–2 для терминов.
- Cross-lesson: `18_gradient_boosting` п. 11 — без изменений.

### Скрипты
- `scripts/rebuild_overfitting_validation_theory.py`
- `scripts/rebuild_overfitting_validation_exercises.py`

### Проверка
- 5 code-ячеек теории выполняются headless (matplotlib Agg).
- `overfitting_validation_practice_solution.ipynb` — выполняется.
