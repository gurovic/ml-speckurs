# REVIEW — Урок 25. Линейная регрессия (теория)

Источник: глубокий разбор Opus 5 (read-only). Правки по этому отчёту **не вносились** — только сохранение.

---

# Ревью: Урок 25. Линейная регрессия (теория)

## 1. Термины: аудит первого появления

| Термин | Ячейка первого появления | Расшифрован там? | Комментарий |
|---|---|---|---|
| регрессия | 0 (цели), 1 | да (яч. 1–2) | «предсказание числа» — хорошо |
| обучение без учителя / *unsupervised learning* | 1 | **да** | русский термин + английский курсивом — эталон |
| обучение с учителем / *supervised learning* | 1 | **да** | |
| классификация | 1 | да | |
| **MAE** | 0 («сравнивать модели по MAE…») | **нет** | и в яч. 13 тоже **нет**: в таблице стоит просто «MAE» без «*Mean Absolute Error* — средняя абсолютная ошибка» |
| **MSE** | 0 | нет | но в яч. 8 расшифрован полностью — ок |
| **$R^2$** | 0 | нет | в яч. 15 расшифрован как «коэффициент детерминации» — ок |
| **RMSE** | 13 | **нет** | нигде в ноутбуке нет «*Root Mean Squared Error* — корень из средней квадратичной ошибки» |
| наклон / смещение ($w$, $b$) | 5 | да | |
| $\hat y$ | 5 | да («игрек с крышкой») — отлично | |
| остаток | 8 | да | |
| функция потерь | 8 | частично | англ. «loss function» дан только в яч. 13, т.е. позже первого использования |
| градиентный спуск | 10 | да | |
| **OLS** / метод наименьших квадратов | 10 («задачу наименьших квадратов») | **аббревиатура не введена** | стоит дать: «МНК, англ. *OLS*» |
| **SGD** (`SGDRegressor`) | 10 | **нет** | «*Stochastic Gradient Descent* — стохастический градиентный спуск» отсутствует |
| скорость обучения / `learning_rate` | 10 («размер шага») / 12 (код) | частично | русское «размер шага» и переменная `learning_rate` нигде не связаны |
| итерация | 10 | нет | |
| **baseline** | 15 | смысл объяснён, **перевода нет** | нужно «базовое (эталонное) решение» |
| adjusted $R^2$ | 15 | да («скорректированный») | |
| train / validation / test | 17 | да | |
| **кросс-валидация** | 17 | **нет** | упомянута дважды (яч. 17, 18) без единого слова о том, что это |
| **утечка данных** (*data leakage*) | 28 («информация из теста просочится») | **термин не назван** | описан механизм, но не имя |
| экстраполяция | 21 | да | |
| `PolynomialFeatures` | 24 (код) | **нет** | в markdown яч. 23 идея объяснена, имя класса — нет |
| **переобучение** | 0, 23, 25 | **нет** | нигде в этом ноутбуке не определено (видимо, опора на прошлые занятия) |
| **недообучение** | 26 | **нет** | |
| регуляризация | 26 | да | |
| **L1 / L2** | 26 | **нет** | «Ridge (L2)», «Lasso (L1)» — что означают L1 и L2, не сказано |
| **Ridge / Lasso / ElasticNet** | 26 | имена без перевода | нет «гребневая регрессия» и т.п. |
| **$\alpha$ / гиперпараметр** | 26 | роль объяснена, **имена — нет** | не сказано «читается „альфа“», слово «гиперпараметр» в ноутбуке отсутствует |
| `StandardScaler`, стандартное отклонение | 28 | **да** | образцовый фрагмент |
| выброс | 30 | да | |
| робастный | 30 | **да** («устойчивые к выбросам») | |
| **`HuberRegressor`** | 30 | **нет** | назван, но что делает — не сказано |
| **RANSAC** (`RANSACRegressor`) | 30 | **нет** | аббревиатура вообще не раскрыта |
| **мультиколлинеарность** | **25** (использование) | **нет** | определение только в яч. 32 — forward reference на 7 ячеек вперёд |
| корреляция | 32 | **нет** | тепловая карта в яч. 33 требует понимания шкалы −1…1 |
| звёздочка `*` в заголовках | 10 | **нет** | помечены разделы 5, 11, 13, 14, 15, 16, но что означает знак — не написано нигде |

**Итого не расшифровано при первом (и вообще ни при каком) использовании:** MAE, RMSE, SGD, OLS, L1/L2, RANSAC, `HuberRegressor`, `PolynomialFeatures`, кросс-валидация, переобучение/недообучение, гиперпараметр, «утечка данных», значение звёздочки.

---

## 2. Findings по ячейкам

### F1 · minor · Ячейка 0: цели занятия используют MAE, MSE, $R^2$ до расшифровки, и не объяснена звёздочка

Три аббревиатуры появляются в списке целей раньше, чем где-либо расшифрованы, а шесть разделов помечены `*` без легенды.

**Fix:** заменить markdown-ячейку 0 целиком:

```markdown
# Занятие 25. Линейная регрессия

Кластеризация помогала **находить группы** в данных, в которых не было готовых ответов. Теперь задача другая: по известным образцам будем **предсказывать число** — цену, температуру, время или результат.

К концу занятия вы сможете:

- отличать регрессию от классификации и кластеризации;
- объяснить, как линейная модель строит прогноз;
- сравнивать модели по трём мерам ошибки — **MAE**, **MSE** и **$R^2$** (что означают эти буквы, разберём в пп. 4, 6 и 7);
- замечать переобучение, использовать регуляризацию и масштабирование;
- замечать выбросы, связанные признаки и нелинейные зависимости.

> **Звёздочка в заголовке.** Разделы, помеченные `*`, — материал повышенной сложности. Их можно прочитать по диагонали при первом проходе и вернуться к ним позже: на понимание остальных разделов это не повлияет.
```

---

### F2 · minor · Ячейка 1: раздел 1 почти дословно дублирует начало раздела 2

Раздел 1 (87 слов) перечисляет «кластеризация / классификация / регрессия», и ровно то же самое повторяется списком в ячейке 2. Читатель дважды читает одно и то же и не получает нового.

**Fix:** заменить markdown-ячейку 1, оставив здесь только ось «с учителем / без учителя» и убрав дубль (сравнение трёх задач остаётся в разделе 2):

```markdown
## 1. Обучение с учителем и без

В задаче **кластеризации** мы работали с данными, в которых не было готовых ответов: алгоритм сам искал похожие объекты. Такие задачи называют **обучением без учителя** (*unsupervised learning*).

Гораздо чаще у нас есть **размеченные данные** — объекты, для которых правильный ответ уже известен. Кто-то (человек, прибор, история продаж) заранее его записал. Это **обучение с учителем** (*supervised learning*): «учитель» — как раз столбец с правильными ответами.

| | Есть готовые ответы? | Что делает алгоритм |
|---|---|---|
| Обучение без учителя | нет | ищет структуру: группы, похожие объекты |
| Обучение с учителем | да | учится повторять известные ответы на новых объектах |

Обучение с учителем делится на два больших случая по тому, **какого типа ответ** мы предсказываем. Их и сравним в следующем разделе.
```

---

### F3 · minor · Ячейка 2: учебный набор из 7 точек подан как настоящие данные

Таблица «часы → балл» появляется без предупреждения, что это крошечный придуманный набор. Позже (яч. 18) ноутбук сам сообщает, что «при семи объектах оценка очень нестабильна» — но ученик к этому моменту уже считает набор нормальным.

**Fix:** добавить в конец markdown-ячейки 2, после таблицы с часами и баллами:

```markdown
Это **учебный набор**: всего 7 учеников, числа придуманы. Для настоящей задачи такого объёма мало — в п. 8 мы увидим, к чему это приводит. Но на семи точках удобно всё пересчитать руками и увидеть каждую ошибку глазами.
```

---

### F4 · major · Ячейка 5: в тексте $w = 6{,}5$, а через одну ячейку код печатает $6{,}96$

Абзац «Если $w=6{,}5$, каждый дополнительный час связан примерно с ростом прогноза на 6,5 баллов» стоит непосредственно перед ячейкой 6, которая печатает `балл ≈ 6.96 · часы + 37.86` (проверено). Формально фраза гипотетическая («если»), но восьмиклассник читает это как ответ и потом не понимает, откуда взялось другое число. Плюс предложение выше обрывается без точки.

**Fix:** заменить markdown-ячейку 5:

```markdown
## 3. Линейная регрессия с одним признаком

График выше подсказывает, что балл зависит от часов подготовки почти **линейно**: все точки лежат близко к некоторой **прямой**.

Простейшая модель как раз и проводит прямую:

$$\hat y = wx + b$$

- $x$ — известный признак (у нас — часы подготовки);
- $\hat y$ (читается «игрек с крышкой») — прогноз модели;
- $w$ — **наклон**: на сколько меняется прогноз, если увеличить $x$ на 1;
- $b$ — **смещение**: прогноз при $x = 0$.

Обучить модель — значит подобрать всего два числа, $w$ и $b$. Сейчас мы поручим это `sklearn` и получим $w \approx 6{,}96$: каждый дополнительный час связан примерно с ростом прогноза на 7 баллов.

⚠️ Слово «связан» здесь не случайно. Это **связь в данных**, а не **доказательство причины**: сама по себе формула не доказывает, что именно часы вызвали рост. Зонтики часто встречаются рядом с лужами, но зонтики не создают лужи — причина и того, и другого — дождь.
```

---

### F5 · major · Ячейка 6: прогноз на 8 часов — это экстраполяция, против которой предупреждает п. 10

Модель обучена на диапазоне 1…7 часов, а ячейка печатает `Прогноз для 8 часов: 93.6` (проверено) без всякой оговорки. В п. 10 ноутбук перечисляет «прогнозы далеко за пределами изученного диапазона (**экстраполяцию**)» как случай, где линейная модель ломается. Ноутбук противоречит сам себе.

**Fix:** заменить код-ячейку 6:

```python
from sklearn.linear_model import LinearRegression

# sklearn ожидает таблицу признаков (строки — объекты, столбцы — признаки),
# а не одномерный массив. reshape(-1, 1) делает из 7 чисел таблицу 7×1;
# -1 означает «посчитай это число сам».
X = hours.reshape(-1, 1)
print('Было (одномерный массив):', hours)
print('Стало (таблица 7×1):')
print(X)

model = LinearRegression()
model.fit(X, scores)

w = model.coef_[0]
b = model.intercept_
print(f'\nФормула: балл ≈ {w:.2f} · часы + {b:.2f}')
print(f'Прогноз для 4.5 часов: {model.predict([[4.5]])[0]:.1f}')
print(f'Прогноз для 8 часов:   {model.predict([[8]])[0]:.1f}  <-- осторожно!')
print('\n8 часов лежит ЗА пределами того, что модель видела (она знает только 1...7).')
print('Такой прогноз называют экстраполяцией, и доверять ему нельзя: см. п. 10.')
```

Выведет (проверено): `балл ≈ 6.96 · часы + 37.86`, прогноз для 4.5 ч — 69.2, для 8 ч — 93.6.

---

### F6 · minor · Ячейка 7: линия модели рисуется на 0…8, зона экстраполяции не отмечена

`grid = np.linspace(0, 8, 100)` — красная прямая уверенно продолжается там, где данных нет, и это никак не показано. Картинка визуально «разрешает» то, что текст п. 10 запретит.

**Fix:** заменить код-ячейку 7:

```python
grid = np.linspace(0, 8.5, 100)

plt.figure(figsize=(7, 4.5))
plt.axvspan(0, 1, color='lightgray', alpha=0.5)
plt.axvspan(7, 8.5, color='lightgray', alpha=0.5, label='здесь данных не было')
plt.scatter(hours, scores, s=90, color='steelblue', zorder=3, label='наблюдения')
plt.plot(grid, model.predict(grid.reshape(-1, 1)), color='crimson', label='линия модели')
plt.xlabel('Часы подготовки')
plt.ylabel('Балл')
plt.title('Линейная регрессия: серые зоны — область экстраполяции')
plt.grid(alpha=0.3)
plt.legend()
plt.show()
```

---

### F7 · major · Ячейка 8: утверждение про квадраты фактически неверно

Написано: «Для маленьких ошибок квадрат сильно уменьшает штраф (0.01 * 0.01 = 0.0001)». Квадрат уменьшает число только если оно **меньше 1**, а это зависит от единиц измерения: та же ошибка — «0,01 балла» или «0,6 % шкалы». Для нашей задачи ошибка в 0,01 балла вообще нереалистична (баллы целые). Суть MSE не в «уменьшении маленьких», а в **относительном** перевесе больших ошибок. Плюс в формуле стоят `*` и десятичные точки вместо `·` и запятых.

**Fix:** заменить markdown-ячейку 8:

```markdown
## 4. Ошибка модели и MSE

Мы провели какую-то прямую. Теперь нужно научиться считать, **насколько сильно мы промахнулись**, — чтобы потом эту ошибку минимизировать.

Для каждой точки есть **остаток** (ошибка):

$$e_i = y_i - \hat y_i.$$

У одних точек остаток положительный (модель занизила), у других — отрицательный (завысила). Просто сложить их нельзя: плюсы и минусы погасят друг друга, и среднее может оказаться нулём даже у совершенно негодной модели. Поэтому ошибки возводят в квадрат и усредняют. Так получается **MSE** (*Mean Squared Error* — средняя квадратичная ошибка):

$$MSE = \frac{1}{n}\sum_{i=1}^{n}(y_i-\hat y_i)^2.$$

Квадрат делает так, что **большие промахи весят непропорционально много**:

| Ошибка модели | Вклад в MSE |
|---|---|
| 1 балл | 1 |
| 2 балла | 4 |
| 5 баллов | 25 |
| 10 баллов | 100 |
| 20 баллов | 400 |

Промах в 20 баллов вдвое больше промаха в 10, но в MSE он весит вчетверо больше. Именно поэтому **две ошибки по 10 баллов** дают вклад $100 + 100 = 200$, а **одна ошибка в 20 баллов** — сразу $400$. Для MSE один крупный провал хуже, чем два средних.

*Можно было бы брать не квадраты, а модули ошибок. Но у функции с модулем в нуле «угол», и обычная производная там не определена. Оптимизировать такие функции всё равно умеют, однако для первого знакомства квадраты удобнее.*
```

---

### F8 · major · Ячейка 9: раздел называется «…и MSE», но на картинке нет ни одного квадрата

График рисует пунктирные отрезки — это остатки $e_i$, а не $e_i^2$. Ключевая геометрическая идея MSE («штраф — это площадь квадрата на этом отрезке») в разделе про MSE не показана, хотя `plan.md` её обещает («Геометрическая интерпретация квадратов ошибок»).

**Fix:** заменить код-ячейку 9:

```python
pred = model.predict(X)
residuals = scores - pred

fig, axes = plt.subplots(1, 2, figsize=(13, 4.5), sharey=True)

# Слева — сами остатки.
axes[0].scatter(hours, scores, s=90, color='steelblue', zorder=3, label='факт')
axes[0].plot(hours, pred, color='crimson', label='прогноз')
for x, y, y_hat in zip(hours, scores, pred):
    axes[0].plot([x, x], [y_hat, y], color='gray', linestyle='--')
axes[0].set_title('Пунктир — остаток $e_i = y_i - \\hat y_i$')

# Справа — те же остатки как СТОРОНЫ квадратов: MSE складывает их площади.
axes[1].scatter(hours, scores, s=90, color='steelblue', zorder=3, label='факт')
axes[1].plot(hours, pred, color='crimson', label='прогноз')
for x, y, y_hat in zip(hours, scores, pred):
    side = y - y_hat
    axes[1].add_patch(plt.Rectangle((x, min(y, y_hat)), abs(side), abs(side),
                                    facecolor='orange', alpha=0.35, edgecolor='darkorange'))
axes[1].set_title(f'Квадраты ошибок: их средняя площадь и есть MSE = {np.mean(residuals**2):.1f}')

for ax in axes:
    ax.set_xlabel('Часы подготовки')
    ax.grid(alpha=0.3)
    ax.legend()
axes[0].set_ylabel('Балл')
plt.tight_layout()
plt.show()

print(f'Самый большой остаток: {residuals[np.abs(residuals).argmax()]:+.1f} балла,')
print(f'и именно он даёт самый большой квадрат: {residuals[np.abs(residuals).argmax()]**2:.1f}')
```

Проверено: MSE = 21.6; наибольший по модулю остаток −4.79 (при 5 часах), его квадрат 22.9.

---

### F9 · minor · Ячейка 10: SGD и OLS не расшифрованы

В абзаце «А что делает sklearn?» вводятся `LinearRegression` и `SGDRegressor`; «SGD» не раскрыт, «задача наименьших квадратов» не связана с общепринятой аббревиатурой.

**Fix:** заменить последний абзац markdown-ячейки 10 на:

```markdown
**А что делает sklearn?** В библиотеке есть две модели для нашей задачи.

- `LinearRegression` решает **задачу наименьших квадратов** (англ. *OLS, Ordinary Least Squares* — «обычные наименьшие квадраты») прямым методом линейной алгебры. Подходит, когда признаков и объектов не слишком много.
- `SGDRegressor` (*Stochastic Gradient Descent* — **стохастический градиентный спуск**) идёт к минимуму шагами, причём на каждом шаге смотрит не на все объекты сразу, а на небольшую случайную порцию. Поэтому он выигрывает на очень больших данных.

Дальше в занятии мы пользуемся `LinearRegression`.
```

---

### F10 · minor · Ячейка 11: `assert` без сообщения, ячейка ничего не говорит о результате проверки

Ячейка называется «Проверим формулу точного решения», но при успехе печатает только свои же числа; совпадение с `sklearn` нужно сверять глазами, а при расхождении `assert` упадёт без объяснения.

**Fix:** заменить код-ячейку 11:

```python
# Проверим: даёт ли формула ровно то же, что sklearn?
x_mean, y_mean = hours.mean(), scores.mean()
w_manual = np.sum((hours - x_mean) * (scores - y_mean)) / np.sum((hours - x_mean)**2)
b_manual = y_mean - w_manual * x_mean

print(f'Вручную по формуле: w = {w_manual:.4f}, b = {b_manual:.4f}')
print(f'Что дал sklearn:    w = {w:.4f}, b = {b:.4f}')

assert np.isclose(w_manual, w) and np.isclose(b_manual, b), 'Формула и sklearn разошлись!'
print('\nСовпало до последнего знака: sklearn решает ту же самую задачу, просто быстрее.')
```

Проверено: обе строки дают `w = 6.9643, b = 37.8571`.

---

### F11 · **blocker** · Ячейка 12: градиентный спуск даёт совсем другой ответ, и ноутбук об этом молчит

Код печатает `Градиентный спуск: w=11.76, b=14.12`, тогда как предыдущая ячейка только что напечатала `w=6.96, b=37.86`. Это **не «почти то же самое»**: MSE решения градиентного спуска равна **134.33** против **21.63** у точного решения — в шесть раз хуже (проверено). За 101 шаг при `learning_rate = 0.01` метод просто не успевает сойтись: 2000 шагов дают `w = 6.97, b = 37.84` (проверено). Ученик видит два разных ответа на одну задачу без единого слова объяснения — это разрушает весь раздел.

Вдобавок график ломается: в `history` попадает MSE, посчитанная **до** обновления весов, и первая точка равна **4534.0** при последней **135.2** (проверено), поэтому на картинке видно только вертикальное падение на первом шаге, а дальше — визуально плоская линия. Подпись «Ошибка уменьшается шаг за шагом» ничем не подтверждается.

**Fix:** заменить код-ячейку 12 целиком:

```python
# Упрощённый градиентный спуск: ищем w и b маленькими шагами.
w_gd, b_gd = 0.0, 0.0     # стартуем из нуля
learning_rate = 0.01      # размер шага
steps = 2000              # столько раз повторим шаг

history = []
for step in range(steps):
    prediction = w_gd * hours + b_gd
    error = prediction - scores
    history.append(np.mean(error**2))            # запоминаем текущую MSE
    w_gd -= learning_rate * 2 * np.mean(error * hours)
    b_gd -= learning_rate * 2 * np.mean(error)

print(f'После {steps} шагов:  w = {w_gd:.2f}, b = {b_gd:.2f},  MSE = {history[-1]:.2f}')
print(f'Точное решение:      w = {w:.2f}, b = {b:.2f},  MSE = {np.mean((pred - scores)**2):.2f}')
print(f'\nА что было раньше?')
print(f'  после 1 шага:    MSE = {history[1]:.0f}')
print(f'  после 100 шагов: MSE = {history[100]:.0f}   <-- ещё далеко от цели!')
print('Спуск приходит в тот же минимум, но ему нужно много шагов.')

plt.figure(figsize=(7, 3.5))
plt.plot(history, color='crimson')
plt.yscale('log')   # без логарифмической шкалы первый шаг (MSE ≈ 4534) сплющит весь график
plt.xlabel('Номер шага')
plt.ylabel('MSE (логарифмическая шкала)')
plt.title('Ошибка уменьшается шаг за шагом')
plt.grid(alpha=0.3, which='both')
plt.show()
```

Проверено, выведет: `После 2000 шагов: w = 6.97, b = 37.84, MSE = 21.63`; `Точное решение: w = 6.96, b = 37.86, MSE = 21.63`; после 1 шага MSE ≈ 249, после 100 шагов ≈ 135.

---

### F12 · major · После ячейки 12: «ландшафт ошибки» существует только как нарисованная SVG-картинка

Раздел 5 показывает вручную нарисованные эллипсы и обещает: «Красные точки показывают последовательные шаги к минимуму». Для наших данных настоящий ландшафт не строится ни разу — это единственная иллюстрация в ноутбуке, которая не подтверждена кодом. При этом построить её тривиально, и она наглядно объясняет, почему спуск такой медленный (узкий «овраг» — тот самый, о котором говорит п. 14).

**Fix:** вставить **новую код-ячейку сразу после ячейки 12** (перед markdown-ячейкой 13 «## 6. Метрики качества»):

```python
# Настоящий «ландшафт ошибки» для наших семи точек.
# Для каждой пары (w, b) считаем MSE и рисуем линии одинаковой ошибки.
w_axis = np.linspace(-2, 16, 200)
b_axis = np.linspace(-5, 65, 200)
WW, BB = np.meshgrid(w_axis, b_axis)
MSE_surface = np.mean((WW[..., None] * hours + BB[..., None] - scores)**2, axis=2)

# Заново повторим спуск, но теперь запомним ВЕСЬ путь.
path_w, path_b = [0.0], [0.0]
w_p, b_p = 0.0, 0.0
for _ in range(steps):
    error = w_p * hours + b_p - scores
    w_p -= learning_rate * 2 * np.mean(error * hours)
    b_p -= learning_rate * 2 * np.mean(error)
    path_w.append(w_p)
    path_b.append(b_p)

plt.figure(figsize=(7.5, 5))
contours = plt.contour(WW, BB, MSE_surface,
                       levels=np.geomspace(MSE_surface.min() + 5, MSE_surface.max(), 12),
                       cmap='Blues_r')
plt.clabel(contours, inline=True, fontsize=7, fmt='%.0f')
plt.plot(path_w[::20], path_b[::20], color='crimson', marker='o', markersize=3.5,
         linewidth=1.2, label='шаги градиентного спуска')
plt.scatter([w], [b], color='black', s=160, marker='*', zorder=5, label='минимум MSE')
plt.xlabel('Наклон w')
plt.ylabel('Смещение b')
plt.title('Линии уровня MSE и путь спуска')
plt.grid(alpha=0.3)
plt.legend()
plt.show()

print(f'Конец пути: w = {path_w[-1]:.2f}, b = {path_b[-1]:.2f}')
print('Обратите внимание на форму «чаши»: она вытянута в узкий овраг.')
print('Поэтому спуск сначала быстро скатывается вниз, а потом долго ползёт вдоль дна.')
print('Как сделать чашу круглой, узнаем в п. 14.')
```

Проверено: путь приходит в `w = 6.97, b = 37.84`; минимум сетки MSE = 21.63 совпадает с точным решением. Картинка действительно показывает узкий вытянутый овраг и красный путь, ныряющий в него и ползущий к звёздочке.

---

### F13 · major · Ячейка 13: MAE и RMSE не расшифрованы, а сравнение метрик не подкреплено арифметикой

В таблице стоят голые «MAE» и «RMSE». Фраза «Если ошибка на 20 баллов намного опаснее двух ошибок по 10, полезна MSE/RMSE» — верное, но абсолютно ничем не подтверждённое утверждение; ученику не показано, что MAE в этих двух случаях **одинакова**, а MSE различается вдвое.

**Fix:** заменить markdown-ячейку 13:

```markdown
## 6. Метрики качества: MAE, MSE и RMSE

MSE может играть две роли: быть **функцией потерь** (*loss function*) во время обучения и **метрикой** после обучения. Формула одна и та же, разница только в том, как мы её используем: в первом случае мы её минимизируем, во втором — просто смотрим на число.

Кроме MSE есть ещё две привычные меры ошибки:

| Метрика | Полное название | Формула | Особенность |
|---|---|---|---|
| **MAE** | *Mean Absolute Error* — средняя абсолютная ошибка | $\frac1n\sum\lvert y_i-\hat y_i\rvert$ | измеряется в тех же единицах, что и ответ (в баллах); мягче относится к редким большим промахам |
| **MSE** | *Mean Squared Error* — средняя квадратичная ошибка | $\frac1n\sum(y_i-\hat y_i)^2$ | сильно штрафует большие промахи; измеряется в **квадратных** единицах (балл²) |
| **RMSE** | *Root Mean Squared Error* — корень из средней квадратичной ошибки | $\sqrt{MSE}$ | так же чувствительна к большим промахам, но снова в баллах, а не в балл² |

Чем они отличаются на деле? Возьмём две модели, каждая ошиблась «в сумме на 20 баллов»:

| | Ошибки | MAE | MSE |
|---|---|---|---|
| Модель А | 10 и 10 | $(10+10)/2 = 10$ | $(100+100)/2 = 100$ |
| Модель Б | 20 и 0 | $(20+0)/2 = 10$ | $(400+0)/2 = 200$ |

**MAE не видит разницы, MSE — видит, и вдвое.** Отсюда правило выбора: если один крупный провал опаснее двух средних (например, ошибка прогноза лекарства или нагрузки на мост) — берите MSE или RMSE. Если важна типичная ошибка в понятных единицах — берите MAE. Универсально «лучшей» метрики нет, её выбирает задача.
```

---

### F14 · minor · Ячейка 14: метрики напечатаны без масштаба, «близко описывает» ничем не измерено

`RMSE = 4.65 балла` — это много или мало? Ученику не с чем сравнить.

**Fix:** заменить код-ячейку 14:

```python
from sklearn.metrics import mean_absolute_error, mean_squared_error, r2_score

# Пока это учебный расчёт на тех же данных, по которым модель обучалась.
mae = mean_absolute_error(scores, pred)
mse = mean_squared_error(scores, pred)
rmse = np.sqrt(mse)     # в старых версиях sklearn был параметр squared=False, теперь берём корень сами

print('Метрики на обучающих данных:')
print(f'MAE  = {mae:.2f} балла')
print(f'MSE  = {mse:.2f} балла²   <-- квадратные баллы, «на глаз» не читаются')
print(f'RMSE = {rmse:.2f} балла   <-- то же самое, но снова в баллах')
print()
print(f'Для сравнения: средний балл в наборе {scores.mean():.1f},')
print(f'а разброс баллов — от {scores.min():.0f} до {scores.max():.0f}.')
print(f'То есть модель типично промахивается примерно на {mae:.0f} балла при размахе {scores.max()-scores.min():.0f}.')
print('\nВывод: модель близко описывает известные точки — но это ещё не проверка на *новых* учениках.')
```

Проверено: MAE = 4.54, MSE = 21.63, RMSE = 4.65, средний балл 65.7, размах 42…84.

---

### F15 · minor · Ячейка 15: заголовок объединяет два концепта, «baseline» не переведён, лишняя запятая

Заголовок «Константный baseline и $R^2$» соединяет через «и» два самостоятельных концепта (`workflow`-baseline и `metrics`-$R^2$), что запрещено правилом. Перенумеровывать разделы нельзя, поэтому меняем формулировку: $R^2$ здесь — не отдельная тема, а **способ измерить превосходство над baseline**, и заголовок должен это отражать. Плюс «Прежде, чем строить» — запятая лишняя, а само слово «baseline» ни разу не переведено на русский.

**Fix:** заменить markdown-ячейку 15:

```markdown
## 7. Насколько мы лучше самого простого прогноза: $R^2$

Прежде чем строить линейную регрессию, ответим на неудобный вопрос: **а с чем мы вообще сравниваем?** Проведём *горизонтальную* прямую, то есть для любого $x$ будем выдавать одно и то же число. Такое нарочито простое решение, с которым сравнивают все остальные, называют **baseline** (читается «бейзлайн», по-русски — базовое, или эталонное, решение).

Для MSE лучший константный baseline — это **среднее значение** $\bar y$: никакое другое одно число не даст меньшую сумму квадратов.

Теперь можно спросить: какую долю ошибки baseline модель сумела убрать? Это и есть **коэффициент детерминации** $R^2$ (читается «эр квадрат»):

$$R^2 = 1-\frac{\sum(y_i-\hat y_i)^2}{\sum(y_i-\bar y)^2}.$$

Сверху — ошибка нашей модели, снизу — ошибка baseline. Отсюда шкала:

- $R^2=1$ — прогнозы идеальны на этом наборе;
- $R^2=0$ — модель ровно не лучше, чем «всегда говорить среднее»;
- $R^2<0$ — модель **хуже** baseline.

Отрицательный $R^2$ вполне возможен **на новых тестовых данных**, которых модель не видела. А вот на обучающих данных обычная линейная регрессия со свободным членом $b$ подбирает коэффициенты не хуже среднего, поэтому там её $R^2$ всегда неотрицателен.

Три предостережения:

1. Универсальной границы «хорошего» $R^2$ нет: в одной области $0{,}5$ — успех, в другой $0{,}95$ — мало.
2. $R^2$ **не означает «процент правильных ответов»**.
3. При добавлении признаков обучающий $R^2$ может расти, даже если новый признак бесполезен. Есть скорректированный $R^2$ (*adjusted $R^2$*) со штрафом за число признаков, но окончательную проверку всё равно проводят на отдельных тестовых данных.
```

---

### F16 · major · Раздел 7 обещает горизонтальную прямую, но никакой прямой не показывает

Текст просит «давайте сделаем прогноз, проведя горизонтальную прямую», а ячейка 16 печатает только три числа. Визуального сравнения «baseline против модели» — которое и есть смысл $R^2$ — в ноутбуке нет.

**Fix:** заменить код-ячейку 16 целиком (она печатает те же числа плюс арифметику $R^2$ и картинку):

```python
baseline = np.full_like(scores, scores.mean())

ss_baseline = np.sum((scores - baseline)**2)
ss_model = np.sum((scores - pred)**2)

print('Сравнение на обучающих данных:')
print(f'MSE baseline: {mean_squared_error(scores, baseline):.2f}')
print(f'MSE модели:   {mean_squared_error(scores, pred):.2f}')
print()
print('Откуда берётся R²:')
print(f'  сумма квадратов ошибок baseline = {ss_baseline:.1f}')
print(f'  сумма квадратов ошибок модели   = {ss_model:.1f}')
print(f'  R² = 1 - {ss_model:.1f} / {ss_baseline:.1f} = {1 - ss_model/ss_baseline:.3f}')
print(f'  проверка через sklearn: {r2_score(scores, pred):.3f}')
print('\nВывод: модель убрала 90% ошибки baseline на известных точках.')
print('Но обобщение на новых учениках проверим отдельно.')

fig, axes = plt.subplots(1, 2, figsize=(12, 4), sharey=True)
for ax, y_hat, name, color in [
    (axes[0], baseline, 'baseline: всегда среднее', 'gray'),
    (axes[1], pred, 'линейная регрессия', 'crimson'),
]:
    ax.scatter(hours, scores, s=80, color='steelblue', zorder=3, label='факт')
    ax.plot(hours, y_hat, color=color, linewidth=2, label=name)
    for x_, y_, yh_ in zip(hours, scores, y_hat):
        ax.plot([x_, x_], [yh_, y_], color='gray', linestyle='--', linewidth=1)
    ax.set_title(f'{name}\nсумма квадратов ошибок = {np.sum((scores - y_hat)**2):.0f}')
    ax.set_xlabel('Часы подготовки')
    ax.grid(alpha=0.3)
    ax.legend()
axes[0].set_ylabel('Балл')
plt.tight_layout()
plt.show()
```

Проверено: сумма квадратов baseline = 1509.4, модели = 151.4, $R^2 = 1 - 151.4/1509.4 = 0.900$ (совпадает с `r2_score`).

---

### F17 · major · Ячейка 17: раздел 8 — «в одну строчку», кросс-валидация упомянута без объяснения

81 слово, три голых буллета, ни схемы, ни числа, ни картинки. Слово «кросс-валидация» вводится здесь и в ячейке 18 как нечто само собой разумеющееся. Главный вопрос ученика — «а сколько данных куда?» — не затронут.

**Fix:** заменить markdown-ячейку 17:

```markdown
## 8. Train / validation / test для регрессии

Оценивать модель на тех же примерах, по которым она училась, нечестно: это как принести на контрольную заранее решённые задачи. Поэтому данные делят на три части **до** начала работы:

| Часть | Как читается | Зачем нужна | Сколько раз используется |
|---|---|---|---|
| **train** | «трейн», обучающая | модель подбирает по ней $w$ и $b$ | много |
| **validation** | «валидация», проверочная | по ней выбираем признаки, регуляризацию и другие настройки | много |
| **test** | «тест», тестовая | одна-единственная финальная проверка выбранного решения | **ровно один раз** |

Типичное деление — примерно 60 / 20 / 20 процентов, но точные доли зависят от размера данных.

Почему test используют **только один раз**? Как только вы посмотрели на test и что-то из-за него поменяли, он перестал быть «новыми данными»: вы уже подстроились под него, пусть и через собственную голову. Это разновидность **утечки данных** (*data leakage*) — когда информация, которой в момент обучения быть не должно, всё-таки просачивается в модель.

> **Кросс-валидация** — приём для случая, когда данных мало и жалко отрезать целую часть под validation. Данные режут на $k$ равных кусков и повторяют обучение $k$ раз: каждый раз один кусок работает проверочным, остальные — обучающими. Итог — среднее по $k$ прогонам, оно устойчивее одной случайной разбивки.

Пример ниже сравнивает train и test для одной заранее зафиксированной модели: мы ничего не выбираем, только смотрим на разрыв.
```

---

### F18 · major · Ячейка 18: вывод не называет настоящую причину большого разрыва

Ноутбук объясняет `MAE на тесте: 12.55` тем, что «при семи объектах оценка очень нестабильна». Настоящая причина конкретнее и куда поучительнее: при `random_state=42` в **train попали часы 3, 4, 5, 6, 7, а в test — 1 и 2** (проверено). Модель обучилась на «многочасниках» и вынуждена **экстраполировать вниз**: она предсказывает 55.2 и 59.9 при настоящих 42 и 48. Это тот же самый эффект, о котором п. 10 говорит отдельно, и он здесь никак не назван.

**Fix:** заменить код-ячейку 18:

```python
from sklearn.model_selection import train_test_split

X_train, X_test, y_train, y_test = train_test_split(
    X, scores, test_size=2, random_state=42
)
check_model = LinearRegression().fit(X_train, y_train)

train_mae = mean_absolute_error(y_train, check_model.predict(X_train))
test_mae = mean_absolute_error(y_test, check_model.predict(X_test))

print(f'В train попали часы: {np.sort(X_train.ravel())}')
print(f'В test  попали часы: {np.sort(X_test.ravel())}')
print()
print(f'MAE на обучении: {train_mae:.2f}')
print(f'MAE на тесте:    {test_mae:.2f}')
print()
for x_, y_true, y_hat in zip(X_test.ravel(), y_test, check_model.predict(X_test)):
    print(f'  {x_:.0f} ч: модель говорит {y_hat:.1f}, на самом деле {y_true:.0f}')

print('\nВывод: тестовая ошибка в четыре раза больше обучающей — но не только из-за «невезения».')
print('Смотрите, ЧТО попало в test: самые маленькие значения часов, 1 и 2.')
print('Модель училась на диапазоне 3...7 и вынуждена экстраполировать вниз — вот она и завышает.')
print('При семи объектах одна случайная разбивка ничего не доказывает: нужны либо')
print('больше данных, либо кросс-валидация.')
```

Проверено: train = [3, 4, 5, 6, 7], test = [1, 2], прогнозы 55.2 и 59.9, MAE 2.96 / 12.55.

---

### F19 · major · Ячейка 19: раздел 9 — 59 слов, и в нём не сказано главное про сравнение весов

Раздел вводит $w_1 \ldots w_p$ и сразу говорит «$w_j$ показывает, как изменится прогноз при росте $x_j$ на 1» — после чего ячейка 20 печатает 0.174 и −0.366, из чего ученик неизбежно (и неверно) заключит, что расстояние важнее площади. Предупреждения, что **веса разных признаков нельзя сравнивать между собой**, здесь нет.

**Fix:** заменить markdown-ячейку 19:

```markdown
## 9. Множественная линейная регрессия

В жизни признаков почти всегда несколько:

$$\hat y=w_1x_1+w_2x_2+\ldots+w_px_p+b.$$

Цену квартиры естественно оценивать сразу по площади, расстоянию до центра и этажу. У каждого признака — свой вес $w_j$; все произведения складываются, к сумме добавляется смещение $b$. Модель по-прежнему **линейна по коэффициентам**: ничего сложнее умножения и сложения внутри неё нет.

Как читать вес $w_j$: «если остальные признаки не меняются, а $x_j$ вырос на 1, прогноз изменится на $w_j$». Это осторожная формулировка, а не доказательство причинности.

⚠️ **Важная ловушка.** Веса разных признаков **нельзя сравнивать между собой напрямую**, потому что они измеряются в разных единицах. Вес «за 1 м²» и вес «за 1 км» — это как сравнивать цену за грамм с ценой за тонну. Через одну ячейку мы на это наступим, а в п. 14 разберём, как чинить.
```

---

### F20 · major · Ячейка 20: вывод подталкивает к неверному заключению «расстояние важнее площади»

Печатаются веса 0.174 и −0.366 (проверено) и вывод «площадь повышает прогноз, а километр понижает». По модулю 0.366 > 0.174, и естественный вывод ученика — «расстояние влияет сильнее». После масштабирования веса становятся 2.96 и −1.28 (проверено), то есть **на самом деле сильнее влияет площадь**. Ячейка оставляет ученика с ошибочным впечатлением.

**Fix:** заменить последние две строки `print` в код-ячейке 20 на:

```python
print('Вывод: при неизменном втором признаке площадь повышает прогноз,')
print('а дополнительный километр до центра — понижает. Это связь, а не доказанная причина.')
print()
print(f'А теперь ловушка: |{distance_weight:.3f}| больше, чем {area_weight:.3f}.')
print('Значит ли это, что расстояние важнее площади? НЕТ.')
print('Один вес — «за 1 квадратный метр», другой — «за 1 километр»; это разные единицы,')
print('и сравнивать их между собой бессмысленно. Как сделать веса сравнимыми — в п. 14.')
```

Проверено: `Вес площади: 0.174`, `Вес расстояния: -0.366`, смещение 2.790.

---

### F21 · major · Ячейка 21: самый короткий раздел ноутбука (49 слов), демонстрируется 1 пункт из 4

Перечислены четыре случая отказа линейной модели (изгибы, пороги, взаимодействия, экстраполяция), а показан ровно один — изгиб. Слова «взаимодействия признаков» даны в кавычках без объяснения того, что это значит на примере.

**Fix:** заменить markdown-ячейку 21:

```markdown
## 10. Когда линейная модель не работает

Прямая — очень жёсткая форма. Вот четыре ситуации, где она подводит.

| Что в данных | Пример | Что делает прямая |
|---|---|---|
| **Изгибы и волны** | площадь круга от радиуса | режет дугу пополам: недооценивает края, переоценивает середину |
| **Резкие пороги** | скидка появляется от 5000 ₽ и ни рублём раньше | сглаживает ступеньку в пологий склон |
| **Взаимодействие признаков** | удобрение помогает, только если есть полив | не может сказать «эффект одного зависит от другого» — у неё каждый вес работает сам по себе |
| **Экстраполяция** | прогноз на 20 часов подготовки, когда видели только 1…7 | уверенно продолжает прямую туда, где данных не было (см. яч. с прогнозом на 8 часов) |

Как это **заметить**, а не угадать? Построить **график остатков**: по горизонтали — признак, по вертикали — остаток $e_i = y_i - \hat y_i$. Если модель поймала закономерность, остатки выглядят как бесформенное случайное облако вокруг нуля. Если в облаке проступает дуга, волна или ступенька — модель что-то пропустила, и это «что-то» осталось в ошибках.

Проверим на данных, где настоящая зависимость — парабола.
```

---

### F22 · major · Ячейка 22: текст обещает «случайное облако» и «дугу», а на графике столбики; выразительный $R^2 = 0{,}002$ не напечатан

Правая панель — `bar`-диаграмма, а текст (и в старой, и в новой редакции) говорит про **облако** точек и **дугу**. Столбики не читаются как облако, и сравнивать их с «нормальным» случаем нечем. Кроме того, прямая на этих данных даёт $R^2 = 0{,}002$ (проверено) — то есть буквально не лучше среднего, — и это число нигде не показано, хотя оно и есть доказательство тезиса.

**Fix:** заменить код-ячейку 22:

```python
x_curve = np.linspace(-3, 3, 30)
y_curve = x_curve**2 + np.random.default_rng(3).normal(0, 0.5, len(x_curve))

line = LinearRegression().fit(x_curve.reshape(-1, 1), y_curve)
curve_pred = line.predict(x_curve.reshape(-1, 1))
residuals_curve = y_curve - curve_pred

print(f'R² прямой на этих данных: {r2_score(y_curve, curve_pred):.3f}')
print('Почти ноль! То есть прямая здесь не лучше, чем «всегда говорить среднее».')

fig, axes = plt.subplots(1, 2, figsize=(12, 4))

axes[0].scatter(x_curve, y_curve, color='steelblue', label='данные')
axes[0].plot(x_curve, curve_pred, color='crimson', linewidth=2, label='линейный прогноз')
axes[0].set_title(f'Данные и слишком простая модель (R² = {r2_score(y_curve, curve_pred):.3f})')
axes[0].set_xlabel('Признак x')
axes[0].set_ylabel('Целевая переменная y')
axes[0].grid(alpha=0.3)
axes[0].legend()

# Остатки точками — так видно форму «облака».
axes[1].scatter(x_curve, residuals_curve, color='tab:orange', s=55, label='остаток')
axes[1].axhline(0, color='black', linewidth=1)
axes[1].set_title('Остатки складываются в дугу, а не в случайное облако')
axes[1].set_xlabel('Признак x')
axes[1].set_ylabel('Остаток: факт − прогноз')
axes[1].grid(alpha=0.3)
axes[1].legend()

plt.tight_layout()
plt.show()

print('\nКак читать правую картинку: точки не разбросаны хаотично вокруг нуля,')
print('а выстроились в дугу — по краям модель занижает, в середине завышает.')
print('Такой узор всегда означает: в данных осталась закономерность, которую модель не поймала.')
```

Проверено: $R^2 = 0.002$, наклон прямой 0.073.

---

### F23 · minor · Ячейка 23: «много степеней легко приводит к переобучению» — вперёд на два раздела, без опоры

Фраза бросается в конце раздела 11, а раздел 12 (переобучение) идёт следом и тоже ничего не показывает. Также нигде не назван класс `PolynomialFeatures`, который тут же используется в коде.

**Fix:** заменить markdown-ячейку 23:

```markdown
## 11. Линейная регрессия умеет строить кривые *

Слово «линейная» относится не к форме графика, а к тому, **как в формулу входят коэффициенты**. Мы вольны придумать любой новый признак и подать его модели.

Добавим к признаку $x$ ещё один — $x^2$:

$$\hat y=w_1x+w_2x^2+b.$$

По $x$ это парабола, но по коэффициентам $w_1, w_2, b$ — всё та же линейная комбинация. Модель по-прежнему только умножает и складывает; кривизну ей «принесли» готовым признаком.

Так же можно добавить $x^3$, синус, логарифм или произведение двух признаков $x_1x_2$ (последнее как раз даёт то самое «взаимодействие» из п. 10).

В `sklearn` за создание степенных признаков отвечает `PolynomialFeatures`: он берёт столбец $x$ и достраивает к нему $x^2$, $x^3$ и так далее до нужной степени.

Главное — **не увлечься**. Чем больше степеней, тем гибче кривая и тем легче она обовьёт случайный шум вместо закономерности. Что при этом происходит, разберём в следующем разделе.
```

---

### F24 · major · Ячейка 24: график без подписей осей и без числа, ради которого он строится

`plt.plot` есть, `title`, `legend`, `grid` есть, а `xlabel`/`ylabel` **отсутствуют** — единственный график в ноутбуке с этим дефектом. И нигде не сказано, насколько модель стала лучше: $R^2$ вырос с $0{,}002$ до $0{,}965$ (проверено) — это и есть весь смысл раздела.

**Fix:** заменить код-ячейку 24:

```python
from sklearn.preprocessing import PolynomialFeatures

poly = PolynomialFeatures(degree=2, include_bias=False)
X_curve_poly = poly.fit_transform(x_curve.reshape(-1, 1))
print('Было признаков: 1 (это x).  Стало:', X_curve_poly.shape[1], '(это x и x²)')

poly_model = LinearRegression()
poly_model.fit(X_curve_poly, y_curve)

print(f'\nR² прямой:                {r2_score(y_curve, curve_pred):.3f}')
print(f'R² модели с признаком x²: {r2_score(y_curve, poly_model.predict(X_curve_poly)):.3f}')
print(f'Веса: при x = {poly_model.coef_[0]:.2f}, при x² = {poly_model.coef_[1]:.2f}')
print('Модель сама нашла, что главный вклад даёт x² с весом около 1 — как в исходной формуле y = x².')

dense = np.linspace(-3, 3, 200).reshape(-1, 1)
dense_poly = poly.transform(dense)

plt.figure(figsize=(7, 4.5))
plt.scatter(x_curve, y_curve, color='steelblue', label='данные')
plt.plot(dense, poly_model.predict(dense_poly), color='darkgreen', linewidth=2,
         label='линейная модель с признаком $x^2$')
plt.xlabel('Признак x')
plt.ylabel('Целевая переменная y')
plt.title('Линейная комбинация новых признаков даёт кривую')
plt.grid(alpha=0.3)
plt.legend()
plt.show()
```

Проверено: $R^2$ 0.002 → 0.965; веса 0.07 при $x$ и 1.01 при $x^2$, смещение −0.01.

---

### F25 · **blocker** · Ячейка 25: раздел 12 — чистая проза без единого числа, графика или кода

Переобучение — один из ключевых концептов курса (`overfitting`), и он единственный в этом ноутбуке изложен как список из трёх утверждений, которые нужно принять на веру. Ни картинки, ни цифры, ни демонстрации. При этом раздел 11 только что построил `PolynomialFeatures` — всё готово для показа. Дополнительно: слово «мультиколлинеарность» использовано здесь впервые со ссылкой «подробнее в п. 16», то есть термин применяется за 7 ячеек до своего определения.

**Fix, часть 1** — заменить markdown-ячейку 25:

```markdown
## 12. Когда линейная модель переобучается

**Переобучение** — это когда модель отлично отвечает на знакомые примеры и заметно хуже на новых. Она выучила не закономерность, а конкретные точки вместе со случайным шумом.

Линейная регрессия кажется слишком простой, чтобы такое случилось, но и с ней это бывает:

1. **Признаков много, объектов мало.** В пределе, когда признаков столько же, сколько объектов, модель может пройти ровно через все точки — и не понять при этом ничего.
2. **Сложные признаки.** Добавили $x^2$, $x^3$, $x_1x_2$ — и кривая стала настолько гибкой, что обвивает каждое случайное отклонение.
3. **Подглядывание в test.** Если решения о признаках и настройках принимались после того, как вы посмотрели на тест, модель косвенно подстроилась и под него.
4. **Сильно похожие друг на друга признаки.** Модель не может решить, кому из них отдать заслугу, и веса становятся огромными и неустойчивыми. Это отдельный сюжет — **мультиколлинеарность**, о ней п. 16.

Проверим второй пункт на опыте: возьмём 10 обучающих точек, лежащих вдоль прямой с небольшим шумом, и будем наращивать степень.
```

**Fix, часть 2** — вставить **новую код-ячейку сразу после ячейки 25** (перед markdown-ячейкой 26 «## 13. Регуляризация»):

```python
from sklearn.metrics import mean_squared_error

rng = np.random.default_rng(7)
# Настоящая зависимость простая: y = 3x + 2 плюс небольшой шум.
x_train_of = np.linspace(0, 1, 10)
y_train_of = 3 * x_train_of + 2 + rng.normal(0, 0.3, 10)
x_test_of = np.linspace(0, 1, 30)          # новые точки из той же закономерности
y_test_of = 3 * x_test_of + 2 + rng.normal(0, 0.3, 30)

degrees = [1, 2, 3, 5, 9]
rmse_train, rmse_test = [], []
for degree in degrees:
    pf = PolynomialFeatures(degree=degree, include_bias=False)
    flexible = LinearRegression().fit(pf.fit_transform(x_train_of.reshape(-1, 1)), y_train_of)
    tr = np.sqrt(mean_squared_error(y_train_of, flexible.predict(pf.transform(x_train_of.reshape(-1, 1)))))
    te = np.sqrt(mean_squared_error(y_test_of, flexible.predict(pf.transform(x_test_of.reshape(-1, 1)))))
    rmse_train.append(tr)
    rmse_test.append(te)
    print(f'степень {degree}: RMSE на обучении = {tr:.3f}, RMSE на новых данных = {te:.3f}')

plt.figure(figsize=(7, 4.5))
plt.plot(degrees, rmse_train, marker='o', label='RMSE на обучении')
plt.plot(degrees, rmse_test, marker='s', label='RMSE на новых данных')
plt.xlabel('Наибольшая степень признака')
plt.ylabel('RMSE')
plt.title('Чем гибче модель, тем шире разрыв между обучением и новыми данными')
plt.grid(alpha=0.3)
plt.legend()
plt.show()

print('\nЧитаем график: синяя линия падает до нуля — на обучении модель отвечает идеально.')
print('Оранжевая при этом растёт: на новых данных становится ХУЖЕ.')
print('Степень 9 при 10 точках — модель прошла ровно через все и выучила шум.')
```

Проверено: степень 1 — 0.194 / 0.266; 2 — 0.192 / 0.257; 3 — 0.166 / 0.255; 5 — 0.124 / 0.277; 9 — **0.000 / 0.356**.

---

### F26 · major · Ячейка 26: L1, L2, $\alpha$ и «гиперпараметр» не расшифрованы; не сказано, что $b$ не штрафуется

«Ridge (L2)» и «Lasso (L1)» — четыре непонятных обозначения подряд. Буква $\alpha$ вводится без «читается „альфа“», слово «гиперпараметр» в ноутбуке отсутствует вообще. Также умолчано, что штраф применяется к весам $w_j$, но **не** к смещению $b$ (иначе модель нельзя было бы сдвинуть по вертикали), — и что регуляризованным моделям масштабирование обязательно.

**Fix:** заменить markdown-ячейку 26:

```markdown
## 13. Регуляризация: ограничитель сложности *

Когда коэффициенты становятся огромными, модель начинает слишком резко реагировать на маленькие изменения признаков: сдвинули признак на копейку — прогноз прыгнул на миллион. **Регуляризация** борется с этим: к функции потерь добавляется ещё одно слагаемое — **штраф за большие коэффициенты**. Теперь модели выгодно не только точно попадать, но и держать веса скромными.

Штраф можно считать по-разному, и от этого зависит характер модели.

| Модель | Штраф | Название штрафа | Что делает с весами |
|---|---|---|---|
| **Ridge** («ридж», гребневая регрессия) | $\alpha\sum w_j^2$ | **L2** — сумма **квадратов** весов | плавно ужимает все веса, но почти никогда не обнуляет |
| **Lasso** («лассо») | $\alpha\sum\lvert w_j\rvert$ | **L1** — сумма **модулей** весов | может обнулить часть весов начисто, то есть сам выбрасывает ненужные признаки |
| **ElasticNet** («эластик-нет») | смесь L1 и L2 | — | пропорцию задаёт параметр `l1_ratio` |

Обозначения L1 и L2 — это просто номера способов измерять «общую величину» набора чисел: L1 — через модули, L2 — через квадраты.

$\alpha$ (читается «альфа») управляет силой штрафа. Это **гиперпараметр** — число, которое модель не подбирает сама, его задаём мы и подбираем по validation:

- слишком маленькая $\alpha$ — штраф почти не действует, всё как без регуляризации;
- слишком большая $\alpha$ — веса задавлены в ноль, модель недообучается (то есть не улавливает даже настоящую закономерность).

Два практических замечания:

- штраф накладывается только на веса $w_j$, но **не** на смещение $b$: иначе мы запретили бы модели просто сдвинуться по вертикали;
- перед регуляризацией признаки почти всегда нужно привести к одному масштабу — почему, в п. 14.
```

---

### F27 · major · Ячейка 27: комментарий фактически неверен, и главный вывод раздела не сформулирован

Комментарий «Два почти одинаковых признака: часы и минуты, переведённые обратно в часы» описывает не то, что делает код: второй столбец — это `hours ± 0.1`, а перевод минут обратно в часы дал бы **точно тот же** столбец. Корреляция признаков 0.9991 (проверено).

Важнее другое: печатаются веса `[-6.19 13.11]`, и ученик остаётся один на один с абсурдом — отрицательный вес у часов подготовки. Ключ к пониманию нигде не сказан: **сумма** весов равна 6.92, что практически совпадает с настоящим наклоном 6.96 (проверено). Модель не сошла с ума — она разложила один наклон на два слагаемых. И при этом $R^2$ у Ridge (0.882) и Lasso (0.903) почти не хуже, чем у модели без штрафа (0.905), то есть скромные веса ничего не стоили по качеству.

**Fix:** заменить код-ячейку 27:

```python
from sklearn.linear_model import Ridge, Lasso, ElasticNet

# Два почти одинаковых признака: часы подготовки и они же, но записанные
# кем-то другим с погрешностью в ±0.1 часа (например, ученик округлял по-своему).
X_related = np.column_stack([hours, hours + np.array([0.1, -0.1, 0, 0.1, -0.1, 0, 0.1])])
print(f'Корреляция этих двух признаков: {np.corrcoef(X_related[:, 0], X_related[:, 1])[0, 1]:.4f}')
print('То есть это практически один и тот же признак, записанный дважды.\n')

for name, estimator in [
    ('Без штрафа', LinearRegression()),
    ('L2 / Ridge', Ridge(alpha=10)),
    ('L1 / Lasso', Lasso(alpha=1)),
    ('ElasticNet', ElasticNet(alpha=1, l1_ratio=0.5)),
]:
    estimator.fit(X_related, scores)
    r2 = r2_score(scores, estimator.predict(X_related))
    print(f'{name:12}: веса {np.round(estimator.coef_, 2)}  сумма весов {estimator.coef_.sum():5.2f}  R² {r2:.3f}')

print(f'\nНастоящий наклон (одна модель на одном признаке): {w:.2f}')
print('Как читать таблицу:')
print('  1. Без штрафа веса дикие: -6.19 и +13.11. Отрицательный вес у часов подготовки — бессмыслица.')
print('     Но их СУММА 6.92 почти равна настоящему наклону 6.96: модель просто разложила')
print('     один наклон на два слагаемых, и разложила как попало.')
print('  2. Ridge поделил вклад пополам: 2.92 и 2.98 — веса стали скромными и осмысленными.')
print('  3. Lasso обнулил первый признак и оставил только второй: отбор признаков сработал сам.')
print('  4. Заметьте: R² почти не изменился. Скромные веса достались нам почти даром.')
print('\nПример показывает поведение коэффициентов, а не выбирает лучшую модель:')
print('для честного выбора alpha и типа регуляризации нужно сравнивать качество на валидации.')
```

Проверено: корреляция 0.9991; без штрафа `[-6.19, 13.11]`, сумма 6.92, $R^2$ 0.905; Ridge `[2.92, 2.98]`, $R^2$ 0.882; Lasso `[0, 6.70]`, $R^2$ 0.903; ElasticNet `[3.14, 3.29]`, $R^2$ 0.897.

---

### F28 · major · Ячейка 28: обещанный «вытянутый овраг» и «сравнение коэффициентов» ничем не подкреплены

Раздел утверждает две вещи: (а) без масштабирования градиентный спуск идёт по вытянутому оврагу; (б) масштабирование «помогает сравнивать коэффициенты». Ни то, ни другое в ноутбуке не показано, а ячейка 29 печатает только «средние 0, отклонения 1».

**Fix:** заменить markdown-ячейку 28:

```markdown
## 14. Почему важно масштабирование *

Один признак может измеряться десятками (этаж), другой — миллионами (стоимость аренды в рублях). Из-за этого возникают две разные неприятности.

**1. Градиентному спуску становится тяжело.** «Чаша ошибки» из п. 5 растягивается в длинный узкий овраг — вы уже видели такой на графике линий уровня. Шаг, безопасный поперёк оврага, оказывается смехотворно мал вдоль него, и спуск ползёт к минимуму сотнями шагов. После масштабирования чаша становится почти круглой, и путь заметно короче.

**2. Веса перестают быть сравнимыми.** Вес «за 1 м²» и вес «за 1 км» живут в разных единицах, поэтому по их величине нельзя судить, какой признак важнее (ровно эта ловушка ждала нас в п. 9). И регуляризация из п. 13 страдает от того же: штраф $\alpha\sum w_j^2$ бьёт по крупным числам, а крупными они оказываются просто из-за неудачных единиц измерения, а не из-за важности признака.

Лечение — **StandardScaler**. Он преобразует каждый признак так, чтобы среднее стало 0, а стандартное отклонение — 1:

$$z = \frac{x - \text{среднее}}{\text{стандартное отклонение}}$$

*Стандартное отклонение* — это типичный разброс значений вокруг среднего: квадратный корень из среднего квадрата отклонений. После такого преобразования все признаки говорят на одном языке: «на сколько типичных разбросов я отклонился от своего среднего».

Когда масштабирование обязательно, а когда нет:

| Модель | Нужно ли масштабировать |
|---|---|
| `LinearRegression` (прямое решение) | для **прогноза** не обязательно — он не изменится; для **сравнения весов** нужно |
| `Ridge`, `Lasso`, `ElasticNet` | да, обязательно: иначе штраф несправедлив |
| `SGDRegressor` и другие градиентные методы | да, обязательно: иначе спуск будет ползти по оврагу |

⚠️ В настоящей задаче `scaler.fit(...)` вызывают **только на обучающей выборке**, а тест преобразуют уже обученным `scaler.transform(...)`. Иначе среднее и разброс теста просочатся в обучение — это та самая утечка данных из п. 8.
```

---

### F29 · major · Ячейка 29: обученная модель `scaled_ridge` нигде не используется, а вывод тавтологичен

`scaled_ridge = Ridge(alpha=1); scaled_ridge.fit(...)` — мёртвый код: результат не печатается и не используется. Выводы «средние 0» и «отклонения 1» — это буквально определение `StandardScaler`, они ничего не доказывают. Ячейка не демонстрирует ни одну из двух польз, заявленных в тексте раздела.

**Fix:** заменить код-ячейку 29 целиком:

```python
from sklearn.preprocessing import StandardScaler

# Берём квартиры из п. 9 — там признаки в совсем разных единицах: м² и км.
features = apartments[['площадь', 'до_центра_км']]
target = apartments['цена_млн']

scaler = StandardScaler()
features_scaled = scaler.fit_transform(features)   # здесь весь набор — только ради демонстрации

print('Средние после StandardScaler:', np.round(features_scaled.mean(axis=0), 6))
print('Стандартные отклонения:      ', np.round(features_scaled.std(axis=0), 6))

raw_model = LinearRegression().fit(features, target)
scaled_model = LinearRegression().fit(features_scaled, target)

print(f'\n{"признак":<16}{"вес БЕЗ масштабирования":>26}{"вес ПОСЛЕ масштабирования":>28}')
for name, raw_w, scaled_w in zip(features.columns, raw_model.coef_, scaled_model.coef_):
    print(f'{name:<16}{raw_w:>26.3f}{scaled_w:>28.3f}')

print(f'\nR² обеих моделей одинаков: {r2_score(target, raw_model.predict(features)):.4f} '
      f'и {r2_score(target, scaled_model.predict(features_scaled)):.4f}')
print('Прогнозы не изменились — изменилась только читаемость весов.')
print()
print('Вот теперь можно сравнивать. БЕЗ масштабирования казалось, что расстояние (0.366)')
print('влияет сильнее площади (0.174). ПОСЛЕ масштабирования видно обратное:')
print('2.96 против 1.28 — площадь важнее более чем вдвое. Первое впечатление было ложным,')
print('его создавали единицы измерения, а не данные.')
```

Проверено: веса без масштабирования 0.174 и −0.366; после масштабирования **2.965 и −1.277**; $R^2$ обеих моделей 0.9980; средние 0, отклонения 1.

---

### F30 · major · Ячейка 30: `HuberRegressor` и `RANSACRegressor` названы, но не объяснены; «сравнить MAE и MSE» — совет без инструкции

Ученику предлагают «сравнить MAE и MSE», не сказав, **что** он должен из этого сравнения понять. Названия двух моделей даны без единого слова о том, как они работают; RANSAC — нерасшифрованная аббревиатура.

**Fix:** заменить markdown-ячейку 30:

```markdown
## 15. Выбросы и робастные методы *

**Выброс** — наблюдение, стоящее необычно далеко от остальных. Из-за квадратов в MSE (п. 4) одна такая точка весит как десяток обычных и способна ощутимо потянуть всю прямую к себе.

Первое, что нужно сделать с выбросом, — **разобраться, откуда он взялся**. Это опечатка оператора? Сломанный датчик? Или редкий, но совершенно настоящий случай? Автоматически удалять выбросы нельзя: иногда именно они и есть самое интересное в данных.

Как **обнаружить**, что выброс портит модель: обучите её и сравните MAE и MSE. Если MAE выглядит приемлемо, а MSE непропорционально велика — значит, общая ошибка набралась не из множества мелких промахов, а из одного-двух огромных. Это и есть след выброса.

Если необычные точки реальны и удалять их нельзя, берут **робастные** (устойчивые к выбросам) модели:

- **`HuberRegressor`** — «Хьюбер». Работает как обычная регрессия, пока ошибка небольшая, но для крупных ошибок переключается с квадрата на модуль. Большой промах перестаёт весить квадратично, и выброс теряет власть над прямой.
- **`RANSACRegressor`** — от *RANdom SAmple Consensus*, «согласие случайной выборки». Многократно строит прямую по случайной горстке точек и оставляет тот вариант, с которым согласилось больше всего наблюдений. Точки, не попавшие в это большинство, объявляются выбросами и в итоговом обучении не участвуют.

Посмотрим, что делает с прямой одна-единственная плохая точка.
```

---

### F31 · major · Ячейка 31: отличный график, но ни одного числа

Картинка показывает, что прямая «наклонилась», но насколько — не сказано. Настоящие числа очень выразительны: наклон падает с **6.96** до **1.82**, тогда как `HuberRegressor` удерживает **6.05** (проверено). Без них тезис держится только на глазомере.

**Fix:** добавить в конец код-ячейки 31, после `plt.show()`:

```python
print(f'Наклон без выброса:              {clean.coef_[0]:.2f} балла за час')
print(f'Наклон обычной регрессии:        {ordinary.coef_[0]:.2f} балла за час  <-- почти обнулился!')
print(f'Наклон HuberRegressor:           {robust.coef_[0]:.2f} балла за час  <-- почти не пострадал')
print()
print('Одна точка из восьми уронила наклон с 6.96 до 1.82 — обычная модель почти перестала')
print('видеть связь между подготовкой и баллом. Huber устоял: он не даёт одному промаху')
print('весить квадратично.')
print()
print(f'MAE обычной модели на нормальных точках: {mean_absolute_error(scores, ordinary.predict(X)):.2f}')
print(f'MAE модели Huber на нормальных точках:   {mean_absolute_error(scores, robust.predict(X)):.2f}')
```

Проверено: наклоны 6.96 / 1.82 / 6.05; MAE на «нормальных» семи точках 11.76 у обычной модели против 4.65 у Huber.

---

### F32 · major · Ячейка 33: мёртвый код и дубль импорта в начале ячейки

Первые три строки:

```python
import pandas as pd
corr = pd.DataFrame(X_related, columns=['часы', 'почти те же часы']).corr()
corr.style.format('{:.3f}')
```

`pandas` уже импортирован в ячейке 20; `corr.style.format(...)` не является последним выражением ячейки, поэтому результат **никуда не выводится**; а сама переменная `corr` через пять строк перезаписывается. Три строки, которые не делают ничего, — ровно тот случай, когда начинающий решает, что он чего-то не понимает.

Кроме того, раздел 16 называет способ диагностики — «проверить, сильно ли меняются веса при небольшом изменении обучающих данных», — но нигде его не показывает, хотя это самый убедительный из трёх.

**Fix:** заменить код-ячейку 33 целиком:

```python
multi_features = pd.DataFrame({
    'часы': hours,
    'минуты': hours * 60,                # тот же признак в других единицах
    'сон': [8, 6, 9, 7, 8, 6.5, 7.5],    # действительно отдельный признак
    'балл': scores,
})
corr = multi_features.corr()

fig, axes = plt.subplots(1, 3, figsize=(16, 4.5))

# Корреляция измеряет силу ЛИНЕЙНОЙ связи и лежит от -1 до +1.
image = axes[0].imshow(corr, cmap='coolwarm', vmin=-1, vmax=1)
axes[0].set_xticks(range(len(corr.columns)), corr.columns, rotation=45, ha='right')
axes[0].set_yticks(range(len(corr.index)), corr.index)
for i in range(len(corr.index)):
    for j in range(len(corr.columns)):
        axes[0].text(j, i, f'{corr.iloc[i, j]:.2f}', ha='center', va='center',
                     color='white' if abs(corr.iloc[i, j]) > 0.65 else 'black')
axes[0].set_title('Матрица корреляций\n(1 — жёсткая связь, 0 — связи нет)')
fig.colorbar(image, ax=axes[0], fraction=0.046, pad=0.04)

axes[1].scatter(multi_features['часы'], multi_features['минуты'], s=75)
axes[1].set_title(f'Коллинеарные признаки (корреляция {corr.loc["часы", "минуты"]:.2f})')
axes[1].set_xlabel('Часы подготовки')
axes[1].set_ylabel('Минуты подготовки')
axes[1].grid(alpha=0.3)

axes[2].scatter(multi_features['часы'], multi_features['сон'], s=75, color='tab:green')
axes[2].set_title(f'Слабая линейная связь (корреляция {corr.loc["часы", "сон"]:.2f})')
axes[2].set_xlabel('Часы подготовки')
axes[2].set_ylabel('Часы сна')
axes[2].grid(alpha=0.3)

plt.tight_layout()
plt.show()

# Третий способ диагностики из списка выше: чуть-чуть трясём данные и смотрим на веса.
print('Слегка меняем баллы (в пределах ±1) и каждый раз переобучаем модель')
print('на двух почти одинаковых признаках из п. 13:\n')
rng_shake = np.random.default_rng(0)
for attempt in range(4):
    y_shaken = scores + rng_shake.normal(0, 1.0, len(scores))
    coefs = LinearRegression().fit(X_related, y_shaken).coef_
    print(f'  попытка {attempt + 1}: веса {np.round(coefs, 2)}   сумма {coefs.sum():.2f}')

print('\nВеса скачут от -2.9 до -10.9 и от 10.1 до 17.5 — при том, что данные почти не менялись!')
print('А сумма весов всё время держится около 6.9. Модель уверена в ОБЩЕМ наклоне')
print('и совершенно не уверена, как поделить его между двумя близнецами.')
print('Именно поэтому такие веса нельзя интерпретировать.')

print('\nТо же самое, но с Ridge(alpha=10):')
rng_shake = np.random.default_rng(0)
for attempt in range(4):
    y_shaken = scores + rng_shake.normal(0, 1.0, len(scores))
    coefs = Ridge(alpha=10).fit(X_related, y_shaken).coef_
    print(f'  попытка {attempt + 1}: веса {np.round(coefs, 2)}   сумма {coefs.sum():.2f}')
print('Вот теперь веса стоят на месте — регуляризация вернула им устойчивость.')
```

Проверено: корреляции часы↔минуты = 1.00, часы↔сон = −0.11, часы↔балл = 0.95. Без регуляризации веса по четырём попыткам: `[-9.85, 16.88]`, `[-9.62, 16.33]`, `[-2.93, 10.14]`, `[-10.88, 17.52]`; суммы 7.02, 6.71, 7.21, 6.64. С `Ridge(alpha=10)`: `[2.97, 3.05]`, `[2.84, 2.91]`, `[3.05, 3.09]`, `[2.81, 2.89]`.

---

### F33 · minor · Ячейка 34: итоговый алгоритм не связан ни с одним разделом теории

Девять шагов сформулированы верно, но ученик не может по ним вернуться к материалу: ни одной ссылки на разделы.

**Fix:** заменить markdown-ячейку 34:

```markdown
## 17. Алгоритм работы над задачей регрессии

Сведём всё занятие в один порядок действий. В скобках — куда вернуться, если шаг непонятен.

1. Понять, какое число предсказываем, в какой момент и зачем *(п. 2)*.
2. Отделить test подходящим способом **до** любого исследования и настройки *(п. 8)*.
3. Исследовать только train: пропуски, распределения, выбросы, связи признаков *(пп. 15, 16)*.
4. Выбрать метрику под смысл задачи и построить константный baseline *(пп. 6, 7)*.
5. Обучить простую линейную регрессию и посмотреть на её метрики *(пп. 3, 9)*.
6. По validation или кросс-валидации подобрать признаки, масштабирование и регуляризацию *(пп. 8, 11, 13, 14)*.
7. Построить график остатков и разобрать отдельные крупные ошибки *(п. 10)*.
8. Зафиксировать выбранный процесс и переобучить его на train + validation.
9. Один раз проверить качество на test и честно описать ограничения *(п. 8)*.

Главное правило всего списка: **шаг 9 выполняется ровно один раз**. Если после него хочется что-то поменять — значит, менять надо было на шаге 6.
```

---

### F34 · major · Конец ноутбука: нет итоговой сводки и глоссария

Ноутбук обрывается на алгоритме. У ученика, прошедшего 17 разделов и три десятка новых слов, нет ни одной страницы, к которой можно вернуться перед практикой.

**Fix:** добавить **новую markdown-ячейку в самый конец ноутбука** (после ячейки 34):

```markdown
## Что запомнить

**Три главных идеи занятия**

1. Линейная регрессия предсказывает **число**, проводя прямую $\hat y = wx + b$. Обучить её — значит подобрать $w$ и $b$ так, чтобы ошибка была минимальной.
2. «Хорошая ошибка» — понятие относительное. Всегда сравнивайте модель с **baseline** и всегда смотрите на **новые** данные, а не только на те, по которым учились.
3. Слово «линейная» — про то, как входят коэффициенты, а не про форму графика. Добавьте признак $x^2$ — и та же самая модель нарисует параболу.

**Словарь занятия**

| Термин | Расшифровка | Смысл в одну строку |
|---|---|---|
| остаток | — | $e_i = y_i - \hat y_i$: на сколько модель промахнулась на одном объекте |
| **MAE** | *Mean Absolute Error* | средняя абсолютная ошибка, в единицах ответа |
| **MSE** | *Mean Squared Error* | средняя квадратичная ошибка; крупные промахи весят непропорционально много |
| **RMSE** | *Root Mean Squared Error* | корень из MSE: та же строгость, но снова в единицах ответа |
| **$R^2$** | коэффициент детерминации | какую долю ошибки baseline модель убрала |
| **baseline** | базовое решение | «всегда говорить среднее»; планка, ниже которой опускаться стыдно |
| **OLS** | *Ordinary Least Squares* | метод наименьших квадратов; решает задачу «по формуле» |
| **SGD** | *Stochastic Gradient Descent* | стохастический градиентный спуск; идёт к минимуму маленькими шагами |
| **L1 / L2** | — | штраф по сумме модулей / по сумме квадратов весов |
| **Ridge / Lasso** | гребневая регрессия / лассо | регуляризация L2 (ужимает веса) / L1 (обнуляет лишние) |
| **$\alpha$** | «альфа» | гиперпараметр силы штрафа; подбирается по validation |
| **мультиколлинеарность** | — | признаки дублируют друг друга, веса становятся неустойчивыми |
| **робастная модель** | устойчивая к выбросам | `HuberRegressor`, `RANSACRegressor` |
| **утечка данных** | *data leakage* | в обучение просочилось то, чего в нём быть не должно |

**Три ошибки, которые совершают чаще всего**

- сравнивать веса признаков, измеренных в разных единицах *(п. 14)*;
- радоваться высокому $R^2$ на обучающих данных *(п. 7)*;
- заглянуть в test, поменять модель и снова проверить на test *(п. 8)*.
```

---

### F35 · minor · Репозиторий: `build_notebooks.py` — устаревший генератор-ловушка

`Урок_25_Линейная_регрессия_Теория/build_notebooks.py` собирает ноутбук с 15 разделами в другом порядке (в нём нет ни раздела «Когда линейная модель переобучается», ни текущей нумерации) и **пишет его в другой файл** — `lesson_1_Урок_25_Линейная_регрессия.ipynb`. Любой, кто запустит скрипт, получит устаревшую версию рядом с актуальной.

**Fix:** либо удалить файл, либо привести его в соответствие с ноутбуком и починить имя выходного файла. Если решено оставить как исторический артефакт, добавить в самое начало `build_notebooks.py`:

```python
"""УСТАРЕЛО. Не запускать.

Этот скрипт собирал ЧЕРНОВУЮ версию занятия 25 (15 разделов, другой порядок)
и писал её в lesson_1_Урок_25_Линейная_регрессия.ipynb.

Актуальный ноутбук — Урок_25_Линейная_регрессия.ipynb — правится напрямую.
Запуск этого скрипта создаст рядом устаревшую копию.
"""
import sys
sys.exit('build_notebooks.py устарел: правьте Урок_25_Линейная_регрессия.ipynb напрямую.')
```

---

### F36 · minor · Репозиторий: `plan.md` разошёлся с ноутбуком

В плане 16 пунктов, в ноутбуке 17 разделов, и порядок другой: в плане регуляризация — п. 12, масштабирование — п. 13, множественная регрессия — п. 10; в ноутбуке это пп. 13, 14 и 9. Раздела «Когда линейная модель переобучается» в плане нет вовсе.

**Fix:** заменить нумерованный список в `plan.md` (строки 3–18) на актуальное оглавление:

```markdown
1. Обучение с учителем и без
2. Задача регрессии
3. Линейная регрессия с одним признаком: наклон и смещение
4. Ошибка модели и MSE. Геометрическая интерпретация квадратов ошибок
5. * Как находится лучшая прямая: прямое решение и градиентный спуск
6. Метрики качества: MAE, MSE и RMSE
7. Насколько мы лучше самого простого прогноза: R²
8. Train / validation / test для регрессии
9. Множественная линейная регрессия
10. Когда линейная модель не работает. Анализ остатков
11. * Линейная регрессия умеет строить кривые
12. Когда линейная модель переобучается
13. * Регуляризация: ограничитель сложности (L1, L2, ElasticNet)
14. * Почему важно масштабирование
15. * Выбросы и робастные методы
16. * Мультиколлинеарность
17. Алгоритм работы над задачей регрессии
```

---

## 3. Общие наблюдения

**Диспропорция «слов много — доказательств мало».** По объёму разделы сбалансированы (49–221 слово), но по доказательности — нет. Из 17 разделов **шесть не имеют ни одного графика**, и три из них делают явно визуальные обещания: раздел 7 просит «провести горизонтальную прямую», раздел 14 говорит про «вытянутый овраг», раздел 12 — про то, как модель «обовьёт случайные отклонения». Ни одно из трёх не показано. Разделы 12 и 17 — вообще без кода.

**Самые слабые разделы по плотности содержания:** 10 (49 слов, 1 из 4 заявленных случаев показан), 11 (57 слов), 9 (59 слов), 15 (61 слово), 12 (71 слово, ноль кода при том, что это ключевой концепт).

**Систематический пропуск последнего шага.** Почти каждая код-ячейка печатает числа, но не говорит, какой из них вывод. Веса `[-6.19, 13.11]` не объяснены суммой 6.92; $R^2 = 0{,}002$ не напечатан; наклон 6.96 → 1.82 не показан; веса 0.174 и −0.366 не сопоставлены с 2.96 и −1.28. Материал для сильных выводов уже посчитан кодом — его просто не выводят на экран. Это самая дешёвая по трудозатратам и самая заметная по эффекту группа правок.

**Терминологическая дисциплина неровная.** Там, где автор её соблюдает, получается образцово (*unsupervised learning*, «игрек с крышкой», «робастные (устойчивые к выбросам)», `StandardScaler` со стандартным отклонением). Но MAE, RMSE, SGD, L1/L2, RANSAC, «гиперпараметр», «переобучение» и значение звёздочки `*` не расшифрованы нигде. Причём MAE и RMSE — это метрики, вынесенные в заголовок раздела 6.

**Внутренние противоречия.** Ноутбук трижды противоречит сам себе: прогнозирует на 8 часов при обучении на 1…7, хотя п. 10 запрещает экстраполяцию; даёт $w = 6{,}5$ в тексте и $6{,}96$ в коде; печатает два несовместимых решения одной задачи (пп. 5 и 11 против ячейки 12).

**Типографика.** В ячейках 8, 10 и 26 дефис используется вместо тире («у других - отрицательная», «Линейная регрессия - один из», «слагаемое - штраф»). В ячейке 8 умножение записано как `0.01 * 0.01` вместо `0{,}01 \cdot 0{,}01`, десятичные разделители — точки вместо запятых (при том, что в ячейке 5 в том же тексте используется правильное `6{,}5`). В ячейке 5 предложение обрывается без точки.

**Соответствие правилам проекта.** Правило `theory-static-tables` соблюдено: все статические таблицы (яч. 2, 13) — markdown, а `pd.DataFrame` в ячейках 20 и 33 обоснованы `fit` и построением графиков. Правило `theory-single-concept-sections` формально нарушено дважды — заголовки «7. Константный baseline и $R^2$» и «15. Выбросы и робастные методы» соединяют «и»-ом. Перенумерация не предлагается (F15 переформулирует заголовок 7; для 15 достаточно оставить как есть — робастные методы там не самостоятельный концепт, а прямое следствие темы выбросов, что новый текст F30 и подчёркивает). Кодировка UTF-8 в порядке, весь текст на русском.

**Техника.** Ноутбук исполняется на sklearn 1.8.0 / numpy 2.3.5 / Python 3.14.0 полностью чисто: ни ошибок, ни `DeprecationWarning`, ни `ConvergenceWarning`. Устаревших API (`squared=False`, `cv='prefit'`, `normalize=`, `base_estimator=`) нет. `mean_squared_error` + `np.sqrt` уже используются правильно. Все сохранённые в ноутбуке значения stdout совпадают с тем, что код выдаёт сейчас, — расхождений «сохранённый вывод против свежего запуска» не обнаружено.

---

## 4. Verdict

**Needs substantial rework** — но не переписывание с нуля.

Обоснование. Каркас занятия хороший: логика разделов выстроена правильно, данные подобраны удачно, часть текста написана образцово (пп. 1, 14, 16), техническая часть современна и работает без единого предупреждения. Это не черновик.

Однако есть **один блокер, который делает раздел 5 обучающе вредным**: ученик видит два разных ответа на одну и ту же задачу (`w = 6.96` и `w = 11.76`, MSE 21.63 против 134.33) и не получает ни слова объяснения. Разумный вывод, который он из этого сделает, — «градиентный спуск даёт неправильный ответ», то есть ровно противоположный истине.

Рядом — второй блокер по педагогике: **раздел 12, посвящённый переобучению, не содержит ни кода, ни графика, ни числа.** Это центральный концепт всего курса, поданный как три утверждения на веру.

Сверх этого — три системных дефекта, каждый затрагивающий по 5–10 ячеек: не расшифрованы MAE и RMSE (вынесены в заголовок раздела); шесть разделов делают визуальные обещания без графиков; почти каждая код-ячейка не доводит вычисление до вывода. Правки нужны в **19 из 35 ячеек**, плюс три новые ячейки. Это не «light touch-ups».

Хорошая новость: почти все исправления — доливка текста и `print`-ов к уже существующему и уже работающему коду. Ни структуру, ни нумерацию разделов, ни датасеты трогать не требуется, поэтому каскада в упражнения, практику и `canvases/ml-theory-concepts-map.canvas.tsx` не будет. Единственное изменение заголовка (п. 7) — переформулировка без перенумерации.

---

## 5. TOP-5 изменений

1. **F11 — починить градиентный спуск (ячейка 12).** Единственный блокер: 2000 шагов вместо 101 дают `w = 6.97, b = 37.84` вместо `11.76 / 14.12`, логарифмическая шкала спасает график, а явное сравнение с точным решением превращает «непонятное расхождение» в главный урок раздела.
2. **F25 — дать разделу 12 доказательство.** Новая код-ячейка со степенями 1, 2, 3, 5, 9: RMSE на обучении падает 0.194 → **0.000**, на новых данных растёт 0.266 → **0.356**. Ключевой концепт курса перестаёт быть утверждением на веру.
3. **F13 + F1 — расшифровать MAE и RMSE и починить сравнение метрик.** Две метрики из заголовка раздела 6 нигде не раскрыты; таблица «10 и 10 против 20 и 0» (MAE одинакова, MSE вдвое различается) делает выбор метрики осязаемым за три строки.
4. **F29 + F20 — довести до конца историю с масштабированием.** Убрать мёртвый `scaled_ridge` и показать, что веса 0.174 / −0.366 после масштабирования становятся **2.96 / −1.28**, то есть первое впечатление «расстояние важнее площади» было ложным. Одна правка закрывает ловушку, расставленную в п. 9, и оправдывает существование п. 14.
5. **F12 — построить настоящий ландшафт ошибки.** Новая ячейка с линиями уровня MSE и реальным путём спуска заменяет нарисованную SVG-картинку доказательством, наглядно объясняет, почему спуск медленный, и заранее мотивирует масштабирование из п. 14.

Сразу следом по отдаче: **F27** (сумма весов 6.92 ≈ настоящий наклон 6.96 — то, что делает раздел 13 понятным), **F16** (график baseline против модели), **F32** (мёртвый код плюс демонстрация неустойчивости весов) и **F34** (итоговая сводка со словарём).

I see that my previous changes are gone—those modified files were from earlier sessions I didn't touch. The `Урок_25` notebook isn't in the modified list, which confirms it's read-only. Now I need to write the report in Russian, being careful with the numbers I'm citing since they've all been verified through execution: the OLS model gives w=6.9643, b=37.8571, and MSE=21.

6276, with MAE=4.54, RMSE=4.65, and R²=0.900. I'm also tracking the baseline metrics, gradient descent results across different configurations, train-test split performance, and comparisons across various regression models including Ridge, Lasso, and Elastic Net, plus their stability under perturbation.

I'm examining how different models handle outliers and overfitting across polynomial degrees, and I've got correlation analysis on the features showing strong relationships between some variables. The environment is running sklearn 1.8.0, numpy 2.3.5, and Python 3.14.0 with no errors or deprecation warnings.

I'm noticing that sections 5, 11, 13, 14, 15, and 16 are marked with asterisks in both the notebook and plan.md, indicating optional supplementary material that was never explained. Now I'll write up the full report.
