# CHANGELOG — 18_decision_tree

## Методическая переработка теории (занятие 35)

### Структура
- Теория перестроена: 13 разделов вместо 18; порядок «критерий разбиения → рекурсия → лист → путь → граница → регуляризация».
- Каждый ключевой термин вводится до использования (корень, лист, порог, impurity, Gini, information gain, `criterion`, гиперпараметры).
- Добавлены таблицы и ручной пример выбора разбиения (п. 4).
- Расшифровка `plot_tree` (gini, samples, value, class) в п. 1.
- Энтропия с формулой и сравнением с Gini (п. 5).
- Отдельный раздел «Путь одного объекта» (п. 8) с `decision_path` и `export_text`.
- Объединены переобучение, гиперпараметры и `ccp_alpha` (п. 10).
- Добавлен код permutation importance рядом с impurity importance (п. 12).

### Синхронизация ссылок
- Упражнения: Gini → **п. 3**, разбиение → **п. 4**, листы → **п. 7**, глубина/pruning → **п. 10**, масштаб → **п. 11**.
- Практика: `plot_tree` → **п. 1**, глубина/pruning → **п. 10**, масштаб → **п. 11**, importance → **п. 12**.

### Скрипт
- `scripts/rebuild_decision_tree_theory.py` — пересборка ноутбука теории.
