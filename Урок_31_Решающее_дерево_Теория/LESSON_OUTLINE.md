# LESSON_OUTLINE — Урок_31_Решающее_дерево_Теория

- **Теория / упражнения:** занятие 31
- **Практика:** занятие 32 (следующий урок)
- **Формула:** уроки идут по порядку с 21

## План теории (15 разделов)

Ноутбук: `Урок_31_Решающее_дерево.ipynb` — **63 ячейки** (39 markdown, 24 code).
Сквозной датасет — «подготовка к экзамену»: 10 объектов, признаки `подготовка` и `сон`, целевая переменная «сдал / не сдал».

| № | Раздел | Что показываем кодом |
|---|--------|----------------------|
| — | Шапка: определение, сквозной пример, схема настоящего дерева | загрузка данных, обучение `DecisionTreeClassifier(max_depth=3)` |
| 1 | Как устроено решающее дерево | `plot_tree` сквозного дерева |
| 2 | Как алгоритм выбирает разбиение | — |
| 3 | Мера неопределённости Джини (Gini impurity) | функция `gini`; кривые Gini и энтропии |
| 4 | Information gain — выигрыш от разбиения | `weighted_gini` и gain для A/B; схема разбиения квадратиками; gain от порога |
| 5 | Энтропия * | — |
| 6 | Рекурсивное построение дерева | три дерева `max_depth` = 1, 2, 3 |
| 7 | MSE как критерий разбиения в регрессии | MSE вручную; ступенчатая регрессия `DecisionTreeRegressor` |
| 8 | Прогноз в листе | `predict_proba` на трёх студентах, включая лист-ничью |
| 9 | Путь одного объекта | обход дерева вручную для двух студентов |
| 10 | Ступенчатая граница решений | граница на сквозном примере; лесенка на диагональной границе |
| 11 | Переобучение дерева | `make_moons`; кривая «глубина vs accuracy»; границы при разной глубине; `min_samples_leaf`; `ccp_alpha` и кривая обрезки |
| 12 | Предобработка данных для дерева | `export_text` для `X` и `X * 100` |
| 13 | Важность признаков | bar chart `feature_importances_`; permutation importance на validation |
| 14 | Почему одиночное дерево редко используют в реальных продуктах | три дерева на bootstrap-подвыборках |
| 15 | Когда одно дерево всё-таки уместно | — |

Разделы со звёздочкой `*` — необязательные при первом чтении.

## Код-ячейки по порядку (индексы в ноутбуке)

1 (данные и обучение), 3 (`plot_tree`), 7 (Gini вручную), 9 (кривые Gini/энтропии), 12 (gain A/B), 14 (схема разбиения), 16 (gain от порога), 20 (рост дерева), 23 (MSE вручную), 25 (ступенчатая регрессия), 28 (`predict_proba`), 31 (путь объекта), 34 (граница на сквозном примере), 36 (лесенка на диагонали), 39 (`make_moons`), 41 (кривая глубины), 43 (границы при разной глубине), 45 (`min_samples_leaf`), 47 (`ccp_alpha`), 49 (кривая обрезки), 52 (масштаб не меняет дерево), 55 (bar chart важностей), 57 (permutation importance), 60 (нестабильность дерева).

## Практика (задания 0–8)

См. `Урок_32_Решающее_дерево_Практика.ipynb` — validation/learning curve, CV, подбор гиперпараметров, итог.

## Артефакты

| Файл | Статус |
|------|--------|
| theory | ✓ 63 ячейки, выполняется без ошибок |
| exercises | ✓ |
| practice | ✓ канонический блокнот с условиями, ответами и LLM-критериями |
| Тест_к_теории.ipynb | ✓ 10 вопросов, номера разделов не цитирует |
