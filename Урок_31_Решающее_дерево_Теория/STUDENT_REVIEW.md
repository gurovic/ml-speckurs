# Student Reader — занятие 31

Вопросы, которые школьник реально задавал по прошлой версии теории, и что изменилось в новой.

| Вопрос школьника | Что было неясно в старой версии | Как отвечает новая версия |
|------------------|----------------------------------|---------------------------|
| Почему схема в шапке не похожа на картинку дерева? | В шапке было нарисовано симметричное дерево с многоточиями, а через две ячейки sklearn строил совсем другое — с листом сразу слева | В шапке нарисовано **настоящее** дерево с порогами 3.5 и 4.5 и подписано, что ветви не обязаны быть одинаковой длины |
| В примере сказано «подготовка ≤ 4», а в дереве стоит 3.5. Где правда? | Пример «на сквозном датасете» приводил пороги 4 и 6, которых в дереве нет | П. 1: реальный трёхшаговый путь студента (7 часов подготовки, 5 часов сна) по порогам 3.5 → 4.5 → 4.5 |
| В корне написано `samples = 100.0%` и `value = [0.5, 0.5]` — это как «полобъекта»? | `plot_tree` вызывался с `proportion=True`, а подпись описывала счётчики объектов | Из `plot_tree` убран `proportion=True`: теперь в корне `samples = 10` и `value = [5, 5]`, ровно как в подписи |
| Что означает цвет прямоугольника? | Про заливку не было сказано ни слова | П. 1: оранжевый — преобладает «не сдал», синий — «сдал», почти белый — поровну |
| В листе `value = [1, 1]`, а прогноз «не сдал». Дерево жульничает? | Ничья 50/50 с уверенным вердиктом никак не объяснялась | П. 1: при равенстве выбирается первый класс; объяснено, что это граница возможностей данных, и что из-за этого листа accuracy на train равна 0.9 |
| Что за «NP-полная задача»? | Термин из теории сложности без объяснения | П. 2: «вариантов больше, чем атомов в комнате, и никакой компьютер их не переберёт» |
| Откуда взялся именно порог 3.5? | Раздел про перебор порогов не показывал ни одной цифры | П. 4: график gain от порога — максимум 0.214 приходится ровно на 3.5, а у признака «сон» лучший результат всего 0.125 |
| Три строки таблицы — это и есть Gini? | Формула плюс три числа, кривой нет | П. 3: график Gini и энтропии как функций доли класса; видно, что форма одинаковая, отличается только высота максимума |
| Что физически происходит при разбиении? | Только числа `gain = 0.5` и `gain = 0.25` без картинки и без комментария | П. 4: схема «родитель → два узла» цветными квадратиками плюс разбор, почему один лишний красный квадратик снижает выигрыш вдвое |
| Как выглядит рекурсия? | 52 слова текста, ни кода, ни картинки | П. 6: три дерева `max_depth` = 1, 2, 3 в ряд; видно, что левая ветвь перестаёт расти сама, а листьев становится 2 → 3 → 4 |
| Как дерево предсказывает числа? | `DecisionTreeRegressor` был упомянут, но ни разу не вызван | П. 7: график ступенчатой регрессии на синусе, 4 листа против 26, и красные зоны за пределами train |
| Что значит «слабая экстраполяция»? | Утверждение без иллюстрации | П. 7: при `x = 6` и при `x = 100` дерево выдаёт одно и то же −0.969 — значение самого правого листа |
| Где обещанные «доли классов»? `predict_proba` выдаёт только 0 и 1 | В демонстрации участвовали два студента, оба попадали в чистые листья | П. 8: добавлен третий студент, попадающий в лист-ничью, — видно 0.5 / 0.5 |
| Почему «вероятность 1.0» — это ненадёжно? | Была фраза «не всегда хорошо откалибрована» без расшифровки | П. 8: единица получена по трём объектам; объяснено, что значит «плохо откалиброваны» |
| Раздел называется «Путь одного объекта», а вопрос всего один | Для демонстрации был выбран объект, попадающий в лист с первого вопроса | П. 9: печатаются два пути — короткий (один вопрос) и длинный (три вопроса) |
| Зачем вообще смотреть путь объекта? | 28 слов, польза не названа | П. 9: объяснимость решения для клиента, юриста и регулятора — то, чего не умеет логистическая регрессия из занятия 27 |
| Где обещанная «лестница»? | График строился на 10 точках и показывал два прямых угла | П. 10: добавлена задача с диагональной границей и три дерева (`max_depth` = 2, 5, 12) — лесенка видна и мельчает с глубиной |
| Какой цвет что означает на графике границы? | Ни легенды, ни подписей зон | П. 10: подписи «зона прогноза «не сдал» / «сдал»» и легенда по классам |
| Почему на графике не видно разреза «подготовка = 3.5»? | Вопрос вообще не поднимался | П. 10: объяснено, что слева и в полосе 3.5–4.5 прогноз одинаковый, поэтому две красные зоны сливаются |
| Где переобучение? Validation ведь растёт | Печатались четыре строки, где validation шла 0.732 → 0.870 и не падала | П. 11: кривая по 15 глубинам, усреднённая по 10 разбиениям: train 0.814 → 1.000, validation достигает 0.880 на глубине 2 и сползает до 0.856 |
| Что такое `make_moons`? | Датасет назывался, но не показывался | П. 11: scatter «двух переплетённых полумесяцев» и размеры train / validation (227 и 123) |
| Что такое post-pruning и `ccp_alpha`? | Одна строка: «чем больше, тем сильнее обрезка» | П. 11: обрезка объяснена через «пользу ветки против цены её листьев», расшифрован *cost complexity pruning*, добавлена кривая обрезки |
| Обрезка помогла или нет? | Самый яркий результат урока стоял в выводе без комментария | П. 11: `ccp_alpha=0.02` оставляет 7 листьев вместо 24 и поднимает validation с 0.870 до 0.902; при 0.08 качество падает до 0.732 |
| `min_samples_leaf` — это про что? | Параметр упоминался в двух разделах, но ни разу не работал | П. 11: цикл по значениям 1, 5, 20, 50 с числом листьев и качеством |
| Что такое `NaN`, `StandardScaler`, `OneHotEncoder`? | Термины шли без расшифровки | П. 12: `NaN` — *Not a Number*, пустая ячейка; расшифрованы оба инструмента и ordinal encoding |
| Правда ли, что масштабировать не нужно? | Утверждение звучало, но не проверялось | П. 12: `export_text` для `X` и `X * 100` — структура совпадает, пороги просто выросли в 100 раз |
| Текст говорит «на validation», а код считает на train | `permutation_importance` вызывалась на тех же 10 объектах, на которых дерево училось | П. 13: permutation importance считается на validation датасета `make_moons`; отдельно сказано, почему на 10 объектах её считать нельзя |
| Почему 0.70 и 0.38 такие разные? | Два числа стояли рядом в таблице без единого слова | П. 13: объяснено, что impurity нормирована в единицу, а permutation — это падение accuracy, и сравнивать надо порядок признаков, а не сами числа |
| Почему дерево «нестабильно»? | Самый сильный аргумент раздела был подан голословно | П. 14: три дерева на трёх подвыборках сквозного датасета — корни разные: «подготовка ≤ 3.5», «сон ≤ 8.5», «сон ≤ 4.5» |
| Что такое «продакшен», «baseline», «feature engineering»? | Английские термины без перевода, «продакшен» стоял прямо в заголовке | Заголовок п. 14 переписан на «в реальных продуктах»; baseline и feature engineering расшифрованы в п. 15 |

Комплект обновлён после ревизии по `scripts/_review_31.md`: теория — 63 ячейки, 15 разделов, 14 графиков.
